# CLAUDE.md

## 必読ドキュメント

### 設計書ハブ

> [!IMPORTANT]
> `docs/design-hub.md` は全設計書のハブドキュメント。いかなるタスク（実装・調査・レビュー・質問への回答）であっても、作業開始前に必ず参照すること。アプリ設計・インフラ設計・テスト方針・ADR等の個別設計書はすべて `docs/design-hub.md` からリンクされている。個別設計書を読む前に必ずハブを経由すること。

### 用語集

- 用語の定義：[docs/reference/glossary.md](docs/reference/glossary.md)

### ポリシー（最優先）

> ポリシーとは、人や組織によって判断が分かれやすい事柄に対して「私たちの考える良い〇〇とはこれです」と宣言するものである。AIの出力のブレを小さくし、意図通りに操ることを目的とする。

ポリシーはこのプロジェクトの憲法であり、すべての判断の北極星である。  
設計・実装・レビュー・git操作など、何らかのアクションを起こす前に必ず `docs/policy-hub.md` を参照し、該当するポリシードキュメントの指針に従うこと。ポリシーに反する提案・実装・操作は行ってはならない。

> [!IMPORTANT]
> **判断を迫られたら（トレードオフ・設計選択・複数解釈・優先順位付けなど）、必ず [docs/policy/refined-engineer-judgment-principles.md](docs/policy/refined-engineer-judgment-principles.md) を参照すること。** これは全ポリシーの上位に立つ「判断の北極星」であり、AIが人間に逐一確認せず自律的に判断するための基準を与える。まずこの原則集に照らして判断し、**原則から明確に答えが出るならAIが自分で決めてよい。判断に迷う余地が残るなら、勝手に決めず人間に確認すること**（「AIへの基本指示」の「正直に振る舞う」参照）。

## AIへの基本指示

- 回答は日本語
- 本プロジェクトは仕様駆動開発を採用。実装を変更する際は必ず `docs/` 配下の設計書の更新要否を判断すること。更新不要と判断した場合もその理由を明示すること（「更新不要・理由：〇〇」）
- **正直に振る舞う（不確実性の扱い）**：もっともらしい推測を断定せず、不確実なら「分からない」と明示する。事実・推測・意見を区別し、コードで確認できることは推測せず読んで確かめる。仕様・設計の判断が分かれる場合や、影響が不可逆な場合（削除・公開・本番操作）は、勝手に決めず `AskUserQuestion` で確認する。
- **後戻りコストの高い意見は、出す前に潰す**：設計方針・技術選定・進め方の相談では、意見を述べる前に `/devil` を確認なしで起動する。反論を受けて意見を変えてよいのは、**確認した事実が根拠のときだけ**である（反論が来たこと自体は根拠にならない）。リポジトリの現物調査（Read, grep）で決着する問いは、`/devil`を使わず現物確認して答える。
- **何よりもまず目的を握る（最優先）**：あらゆるタスクで、着手前に「何のための作業か（目的）」を**他の何よりも先に**確認する。目的は全判断の上位基準だから（目的を見失えば、速く進むほど遠ざかる）。**着手前に、目的を自分の言葉で一文にして必ず出力する**（頭の中で済ませない——取り違えは外に出して初めて人間が正せる）。そのうえで：
  - **後戻りコストが高い**、または**一文に複数の解釈が入りうる**なら → `AskUserQuestion` で認識を確認し、**肯定を得るまで**実装・編集・ファイル作成に進まない。
  - そうでなければ → 「**目的：〇〇。この前提で進めます**」と宣言してから着手する（人間が割り込んで正せるように）。
  - 例外：タイポ修正や自明な単発コマンドなど、取り違えても損害が軽微な作業は宣言のみでよい。
  - 「一文に**できるか**」を内心で自己判定して確認を省くのは禁止（LLM はほぼ常にもっともらしい一文を作れてしまい、確認が永久に発火しない）。ゲートは「一文を**外に出したか**」という観測可能な行動に置く。
- **質問には回答で、指示には作業で応える**：明確な作業指示（「対応開始して」「作業して」「直して」「やって」等）がある場合のみ着手する。それ以外はすべて相談として扱い、回答だけ返す。
- **質問には結論の一文から答える**：回答の第一文を、聞かれたことへの答えにする。はい／いいえ・二択で答えられる問いは、第一文でそう答え切る。Skill が冒頭に別の要素（論点マップ等）を置けと指示していても、答えより前には出さない——先頭を前置きや分析に使うと、読み手が答えを探す羽目になる。
- **`AskUserQuestion` の選択肢は単体で読めるように書く**：question・option の label・description は、**応答本文とは別の UI パネルに表示される**。だから本文を読まなくても意味が通るよう、対象を毎回名指しして書き下す。会話に依存する指示語（本文で定義した略記・「上記の案」「先ほどの3案」など）を選択肢に持ち込むと、ユーザーはパネルだけを見て何を選ぶのか判断できず、意思決定が止まる。
- **割れ窓を見つけたら可視化する（boy-scout）**：いま依頼されている作業の本筋から外れる品質課題（悪い設計・誤った意思決定・質の低いコード・放置されたTODOなど＝割れ窓。**横展開で見つけた関連課題も含む**）に気づいたら、その場で直そうとせず（脱線・コンフリクトを避ける）、`/quick-issue` で**確認なしに** GitHub Issue を起票する。起票は「直す」ことではなく「割れ窓を見えるようにして失わない」ための措置である。**いまの作業と直接関係する問題は割れ窓ではない**——自分の変更が壊したもの・今回の変更の一部として直すべきものは、起票せずその場で解決する。
- **指摘されたら原因を分析して起票する**：人間から問題を指摘されたら、その場の修正で終わらせない。**なぜ起きたかを一段掘る**——どの指示が発火しなかったか、そもそもどこにも書かれていなかったか、書かれていたが判断を誤らせたか。掘った原因と再発防止案を `/quick-issue` で確認なしに起票する。指摘は割れ窓の最も確実な検出器であり、直して終わりにすると次のセッションで同じ失敗が起きる。回答末尾の提案で済ませない——提案は流れて消えるが、Issue は残る。
- **他でも効く知見を得たら起票する**：作業中に「この失敗は他の場面でも起きる」「この手順は Skill や SubAgent にすれば速い」と気づいたら、回答末尾の提案で済ませず、`/quick-issue` で確認なしに起票する。気づきが無かった回は何も書かなくてよい（無理に絞り出さない）。
- **横展開して考える**：修正・追加を指示されたら、その箇所だけで終わらせず、**同じ対応が他にも必要ないか**を横展開して調べる（1つの変更は系全体に波及する）。例：要件定義に SLI/SLO を足したら、監視ポリシーの更新も要らないか確認する。見つけた課題は種類で振り分ける：**割れ窓は提案で止めず boy-scout で自律起票する**（↑参照）。提案・確認に留めるのは、設計方針の選択など**人間の判断を要するもの**だけ。
- **短く明快に書く**：設計書・CLAUDE.md・回答などあらゆる文章を、短くシンプルに、中学生でも追える構造で書く（専門用語は使ってよい）。書く前に核を一文で決め、削除テスト（消して読者が困らない語・行は消す）を当てる。基準は [documentation-policy.md](docs/policy/documentation-policy.md)。
- **返事は短くする**：前置き・注意書き・断り書きは短くし、本題に紙幅を使う。説明を求められたら、詳細を明示的に求められない限り要約で返す。
- **作業中の報告は「重要な発見」と「方向転換」だけ**：最初のツール実行の前に、これからやることを一文で言う（目的の宣言と兼ねてよい）。作業中は、重要なことが分かったときと進む方向を変えるときだけ短く報告する。終わったら結論から書く——最初の一文が「何が起きたか」「何が分かったか」に答え、根拠はその後ろに置く。
- **SubAgentは決められた場面でだけ使う**：各Skillが名指しで起動するもの（`code-reviewer-agent` 等）と、ユーザーが明示的に求めた場合だけに限る。自分で数回のツール実行で終わる調べ物を投げない。自分の作業を点検し直させる目的で追加起動しない。

> [!IMPORTANT]
> Mermaid図（フローチャート・シーケンス図・アーキテクチャ図・アクティビティ図・デプロイ図など）を作成・更新する際は、必ず `/design-doc-mermaid` スキルを使用すること。設計書・ドキュメント・回答内で「図を描く」「diagramを作る」「コードから図を起こす」必要が生じた時点で、他の作業に着手する前にこのスキルを呼び出す。

## 開発フロー

仕様駆動で開発する。各ステップの説明は [docs/guide/development-flow.md](docs/guide/development-flow.md) にある。

> [!IMPORTANT]
> **issue番号だけで開発フローを自動追従する**：「issue 111 対応して」のように issue 番号のみ渡されたら、`gh issue view 111` で本文を取得し、**「実装フロー（使用するSkill）」セクションに記載された Skill を記載順に実行する**。同セクションが無い issue は、変更種別から下表を判断して実行する。

| 変更種別                                                         | 使用Skill                                                             |
| ---------------------------------------------------------------- | --------------------------------------------------------------------- |
| 仕様相談                                                         | 複雑な仕様→/grill-me→/to-plan→/to-issues                              |
| 設計書                                                           | 直接編集→`/quick-doc-review`・新規・大幅加筆は`/design`               |
| アプリ実装                                                       | 直接編集→`/code-review`新規・大幅加筆は`/code-dev`                    |
| CDK                                                              | 直接編集→`/cdk-review`新規・大幅加筆は`/cdk-dev`                      |
| ハーネスのプロンプト定義（`.claude/skills/`・`.claude/agents/`） | 直接編集→`/quick-doc-review`・新規・大幅加筆は `/doc-review`          |
| 要件定義書                                                       | 直接編集→`/quick-doc-review`・新規・大幅加筆は `/requirements-review` |

## 開発ガイドライン

### 実装時の共通ルール

- （厳守！）作業はGitHub Issueに記載されている「タスク一覧」を１つずつ確認し、タスクが完了するごとに必ず `gh issue edit` コマンドでGitHub Issueのチェックリストを更新しながら作業をしてください
- **タスク一覧とポリシーが衝突したらポリシーが勝つ**：Issue のタスク・完了条件がポリシーに反する作業を指示していたら、タスクに従ってはならない。着手前に `gh issue comment` で「どのポリシーのどの規定に反するか」を書き、タスクを訂正してから進める。ポリシーは全判断の上位基準であり、Issue は個別の作業指示だから下位に置く。
- **closeする前にコミット済みを確認する**：`gh issue close` の直前に `git status` を実行し、未コミットの変更が残っていないことを確認する。残っていればコミットしてからcloseする。closeは「作業が残っていない」という宣言だから、作業ツリーに成果物が浮いたままcloseすると、誰も追わない未完了状態が生まれる。
- Issueの作業が完了したら、確認を挟まず `gh issue close` で自律的にクローズすること

### コーディング規約・技術ルール

言語・フレームワーク別のルールは `.claude/rules/` 配下にあり、対象ファイルを読むと自動ロードされる。新規ファイルを書き起こすときは発火しないので、その場合だけ該当ルールを自分で読む。
