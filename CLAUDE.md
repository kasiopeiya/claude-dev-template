# CLAUDE.md

## 必読ドキュメント

@docs/project-context/project-claude.md

- **設計書ハブ**：どのタスクでも着手前に `docs/design-hub.md` を読む。個別設計書はハブから辿る
- **用語集**：用語の意味に迷ったら [docs/project-context/glossary.md](docs/project-context/glossary.md) を引く
- **ポリシー（最優先）**：何かを起こす前に `docs/policy-hub.md` で該当ポリシーを読み、従う。反する提案・実装・操作はしない

> [!IMPORTANT]
> **（AI・必須）** **判断を迫られたら（トレードオフ・設計選択・複数解釈・優先順位付けなど）、必ず [docs/policy/refined-engineer-judgment-principles.md](docs/policy/refined-engineer-judgment-principles.md) を参照する。** 全ポリシーの上位に立つ判断の北極星である。原則から明確に答えが出るならAIが自分で決めてよい。迷う余地が残るなら `AskUserQuestion` で人間に確認する。

## AIへの基本指示

- 回答は日本語
- **何よりもまず目的を握る（最優先）**：目的は全判断の上位基準である（見失えば、速く進むほど遠ざかる）。着手前に、目的を自分の言葉で一文にして必ず出力する（取り違えは外に出して初めて人間が正せる）
  - 後戻りコストが高い、または一文に複数の解釈が入りうる → `AskUserQuestion` で確認し、肯定を得るまで実装・編集に進まない
  - それ以外 → 「**目的：〇〇。この前提で進めます**」と宣言して着手する。タイポ修正など取り違えの損害が軽微な作業は宣言のみでよい
  - 「一文にできるか」を内心で判定して出力を省くのは禁止（LLM はほぼ常にもっともらしい一文を作れるため、確認が永久に発火しない）
- **質問には回答で、指示には作業で応える**：「対応して」「直して」「やって」等の明確な作業指示があるときだけ着手する。それ以外は相談として回答だけ返す
- **正直に振る舞う**：推測を断定せず、不確実なら「分からない」と言う。事実・推測・意見を区別し、コードで確かめられることは読んで確かめる。不可逆な操作（削除・公開・本番操作）の前は `AskUserQuestion` で確認する
- **重要な相談は３人で議論する**：設計方針・技術選定・進め方の相談では、意見を述べる前に `/devil` を確認なしで起動する（1セッション1回まで）。現物を読めば決着する問いには使わない
- **`AskUserQuestion` の選択肢は単体で読めるように書く**：question・label・description は本文と別パネルに出るので、本文を読まずに選べないと意思決定が止まる。対象を毎回名指しし、本文で定義した略記や「上記の案」を持ち込まない
- **横展開して考える**：1つの変更は系全体に波及する。修正・追加を指示されたら、同じ対応が他にも要らないか調べる。提案・確認に留めるのは、設計方針の選択など人間の判断を要するものだけ
- **本筋外の課題は起票する（boy-scout）**：次のものはその場で直さず（脱線・コンフリクトを避ける）、提案でも済ませず、`/quick-issue` で確認なしに起票する（提案は流れて消えるが、Issue は残る）
  - 割れ窓：悪い設計・誤った意思決定・質の低いコード・放置された TODO
  - 人間からの指摘：直したうえで、なぜ起きたか（指示が発火しなかった／どこにも無かった／判断を誤らせた）を一段掘り、再発防止案と一緒に起票する。直して終わりにすると次のセッションで同じ失敗が起きる
  - ただし**いま編集したファイル内の問題は、自分が壊したものも元からの不備も起票せず、その場で直す**。判定は原因ではなく、編集したパスと一致するかで行う（原因の判断を挟むと基準が甘くなる）
- **短く明快に書く**：どの文章も、中学生でも追える構造で書く（専門用語は可）。書く前に核を一文で決め、削除テスト（消して読者が困らない語・行は消す）を当てる。基準は [documentation-policy.md](docs/policy/documentation-policy.md) と [document-writing.md](.claude/rules/document-writing.md)
- **返事は結論から短く**：第一文で聞かれたことに答え切る（二択ならそう答える）。Skill が冒頭に別の要素を置けと指示していても、答えより先に出さない（読み手が答えを探す羽目になる）。前置き・断り書きは短くする。作業中は、重要な発見と方向転換に加え、長く黙る前に「いま何をしているか」を一行で伝える
- **SubAgent は決められた場面でだけ使う**：Skill が名指しするものと、ユーザーが明示的に求めた場合だけ。数回のツール実行で終わる調べ物や、自分の作業の点検し直しには使わない

## 開発フロー

仕様駆動で開発する（[docs/guide/development-flow.md](docs/guide/development-flow.md)）。実装を変えたら `docs/` の設計書の更新要否を判断し、不要なら理由を書く（「更新不要・理由：〇〇」）。

> [!IMPORTANT]
> **（AI・必須）** **issue番号だけで開発フローを自動追従する**：「issue 111 対応して」のように番号だけ渡されたら、`gh issue view 111` の「実装フロー（使用するSkill）」に書かれた Skill を記載順に実行する。同セクションが無ければ、変更種別から下の一覧で決める。

変更種別ごとの使用Skill（直接編集した場合／新規・大幅加筆の場合）：

- 仕様相談：複雑な仕様なら `/grill-me` → `/to-plan` → `/to-issues`
- 設計書：`/quick-doc-review`／`/design`
- アプリ実装：`/code-review`／`/code-dev`
- CDK：`/cdk-review`／`/cdk-dev`
- ハーネスのプロンプト定義（`.claude/skills/`・`.claude/agents/`）：`/quick-doc-review`／`/doc-review`
- 要件定義書：`/quick-doc-review`／`/requirements-review`
- ポリシー（`docs/policy/`）：`/quick-doc-review`／`/doc-review`
- ADR（`docs/adr/`）：`/quick-doc-review`／`/create-adr`
- 上記以外の文書（`docs/` 配下・`CLAUDE.md`）：`/quick-doc-review`／`/doc-review`

## Issue 作業の共通ルール

- Issue 番号の無い作業指示を受けたら、着手前に Issue を起票する（相談を経た作業は `/to-issues`、その場の指示は `/quick-issue`）。小さい作業も例外にしない
- **（厳守）** Issue の「タスク一覧」を1つずつ進め、完了するごとに `gh issue edit` でチェックを更新する
- タスク一覧がポリシーに反していたら従わない。着手前に `gh issue comment` でどのポリシーのどの規定に反するかを書き、タスクを訂正してから進める
- 完了したら確認を挟まず `gh issue close` する。close は「作業が残っていない」という宣言なので、直前に `git status` で未コミットの変更が無いことを確かめ、残っていればコミットしてから close する
- AI が `git push` したら、続けて `/pr-body <Issue番号>` を実行する
