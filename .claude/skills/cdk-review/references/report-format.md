# レビューレポートフォーマット

`/cdk-review` が Phase 3 で、各レンズの結果を統合して出すときの書式。観点の判定基準・severity の定義・合否の計算方法は SKILL.md と review-criteria.md に置く（本ファイルには転記しない）。

点数・総合評価・良い点は、どの欄にも書かない。結果を使う側（`/cdk-dev` など）は指摘の有無と severity だけで直すかを決めており、良い点を求めると、見る観点の少ないレビューでは良い点のでっち上げを招く。

## レポート全体

出力順は次のとおり：ヘッダー（判定した観点・未判定の観点・合否）→ ファイルごとのレポート×N → 全体サマリー（複数ファイルのとき）→ 設計書レビュー（対象があるとき）。「レビュー完了」はレポート全体の最後に1回だけ出す。

```
## CDK Review Report

### 🔍 レンズごとの判定した観点

<!-- レンズごとに1行。各レンズの生レポートにある「判定した観点」をそのまま転記する。転記漏れがあると、次の「未判定の観点」が信頼できなくなる -->

| レンズ | 判定した観点 |
| --- | --- |
| <レンズ名> | <列挙> |

### ❓ 未判定の観点

<!-- review-criteria.md の観点一覧の全行＋観点一覧の外（サービス設定値（セキュリティ）・サービス設定値（運用・性能））のうち、上の表のどこにも名前が挙がっていないものを列挙する。対象設計書がある場合は iac-infra-design-doc-policy.md「レビュー観点」も対象に含める。無ければ「なし」。これはレビュー対象の欠陥ではなく、レンズの割り方の穴 -->

- <観点名>（無ければ「なし」）

### 🚦 合否

<!-- 判定基準・数え方は SKILL.md「Phase 3: 統合してレポートを出す」手順7のとおり -->

- **判定**: <🚫 要修正 / ✅ 合格>
- **Critical残数**: <手順7の計算による件数>
- **Issue にして残す Critical**: <issue:needs-human-decision として起票した Issue 番号と概要。無ければ「なし」>

---

<!-- 対象ファイルごとに、以下を繰り返す -->

## ファイル: `<ファイルパス>`

### 📁 ファイル情報
- **種別**: <App / Stack / Construct / Parameter>
- **最終更新**: <git log による最終コミット情報>

### 📋 観点ごとの判定

<!--
review-criteria.md の観点一覧を上から順に全行書き出す（行を省かない）。観点名は一覧の行名と一字一句同じにする。
判定は 準拠 / 違反 / 対象外 / 未判定 のいずれか。違反の行には、下の「改善が必要な点」に出した指摘の数を書く。
-->

| 観点 | 判定 | 指摘の数 |
| --- | --- | --- |
| <観点一覧の行名> | <準拠 / 違反 / 対象外 / 未判定> | <違反なら件数、それ以外は -> |

---

### 🔧 改善が必要な点

#### 🚨 Critical

- **<観点名>**: <具体的な問題の説明>
  - **場所**: `<ファイルパス>:<行番号>`
  - **引用**: `<対象ファイルからコピーしたコード1〜3行。欠落の指摘なら、欠落を確認した範囲のコード>`
  - **提案**: `<改善後のコード例。レンズ間で食い違った場合は裁定で採った案を書き、捨てた案と根拠の条文を添える>`
  - **理由**: <なぜこの改善が必要か>

#### ⚠️ High

- **<観点名>**: <具体的な問題の説明>（場所・引用・提案・理由は Critical と同じ4点を書く）

#### 💡 Medium

- **<観点名>**: <具体的な提案>（場所・引用・提案・理由は Critical と同じ4点を書く）

#### ❓ 確認点（Review Questions）
<!-- 観点一覧の外。担当レンズは cdk-reviewer-agent「レンズ一覧と担当観点（正典）」のとおり -->

- **<サービス設定値（セキュリティ）／サービス設定値（運用・性能）>**: <具体的な確認内容>
  - **場所**: `<ファイルパス>:<行番号>`
  - **現状**: `<現在の設定や実装>`
  - **確認事項**: <何を確認してほしいか・どのような懸念があるか>

---

### 🚀 次のステップ（優先度付き）

<!-- 優先度は指摘の severity から決める（Critical→高・High→中・Medium→低）。指摘が無ければ「なし」と書く -->

1. **[高] <Critical の指摘の観点名>**: <具体的なアクション>
2. **[中] <High の指摘の観点名>**: <具体的なアクション>
3. **[低] <Medium の指摘の観点名>**: <具体的なアクション>

---
```

## 全体サマリー（複数ファイルをレビューしたとき）

各ファイルレポートをすべて出した後に1回だけ出す。

```
## CDK Review Summary

| ファイル | 種別 | Critical | High | Medium |
| --- | --- | --- | --- | --- |
| `<ファイルパス>` | <種別> | <件数> | <件数> | <件数> |

### 🚀 次のステップ（全ファイル横断・優先度付き）

<!-- 各ファイルの「次のステップ」を severity 順（Critical→High→Medium）に並べ直す -->
```

---

## 設計書レビュー（対象がある場合のみ）

<!-- docs/policy/iac-infra-design-doc-policy.md を基準に、「設計書の内容」レンズがインフラ設計書・IaC 設計書の「内容の妥当性」を評価した結果。文書ごとに1ブロック出す。コードの指摘とは別の欄に出すが、Critical は 🚦 合否の Critical 残数に含める。文章・構成の質は /doc-review の領分 -->

```
## 📐 設計書レビュー
- 対象: [対象設計書のパス一覧／対象なしなら「対象なし・スキップ」]

<!-- 対象設計書ごとに、以下を繰り返す -->

### 設計書: `<パス>`

#### ✅ 妥当性チェック
<!-- iac-infra-design-doc-policy.md「レビュー観点」の該当節（両設計書共通＋その文書固有）の項目を ✅/⚠️/❌ + 詳細 で列挙する。項目はポリシーを SSOT とし、ここに転記しない -->
- ✅ / ⚠️ / ❌ [ポリシーの観点]: [詳細]

#### 🔧 内容面の指摘
<!-- ⚠️・❌ を付けた項目ごとに1件出す。severity は review-criteria.md「severity の決め方」の基準で個別に判定する（項目名で機械的に決めない） -->

##### 🚨 Critical
- [場所・引用・提案・理由を書く]

##### ⚠️ High
- [場所・引用・提案・理由を書く]

##### 💡 Medium
- [場所・引用・提案・理由を書く]
```

---

**レビュー完了**
