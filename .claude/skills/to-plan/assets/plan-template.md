# <Plan タイトル：何をするかが一読でわかる説明的な見出し>

> [!NOTE]
> 対象読者：このPlanからIssueをつくるAI（`/to-issues`）と、実装を引き継ぐ別セッションの実装者。

## TL;DR

<何を・なぜそう決めたかを最大3行で。4行になったら本文。仕様が固まっていない兆候。>

## 背景・目的（なぜやるのか）

<解決する課題・ビジネス上の背景・この変更がもたらす価値。HOWではなくWHY。実装者がこれを読めば、判断に迷ったとき「何を優先すべきか」を自力で決められる粒度で書く。起点Issueがあればリンクする。>

## grill-me で確定した仕様

<`/to-issues` がそのまま転記するセクション。決定事項だけでなく却下した代替案も書き、別セッションでの蒸し返しを防ぐ。grill-me 未実施なら「grill-me 未実施」と明記。>

- 確定した仕様：
- 設計判断とその根拠（なぜその案にしたか）：
- 却下した代替案とその理由：

## スコープ

|              | 内容                             |
| ------------ | -------------------------------- |
| やること     | <この Plan に含む>               |
| やらないこと | <あえて含めない。線引きの理由も> |

## 設計書・要件定義への影響（必須・省略禁止）

<更新が必要な設計書を列挙。`docs/design-hub.md` 起点で確認する。あわせて `docs/requirements.md`（要件定義）の更新要否も判断する——通常は実装が要件に従うため変更不要。設計・実装で要件の誤り・不足・変更が判明した場合のみ更新対象に挙げる。更新不要なら「更新不要・理由：〇〇」と必ず明記する。>

| 文書                       | 更新内容                                             | 理由   |
| -------------------------- | ---------------------------------------------------- | ------ |
| 例：docs/.../xxx-design.md | <何を変えるか>                                       | <なぜ> |
| 例：docs/requirements.md   | <要件の追加・修正。不要なら「更新不要・理由：〇〇」> | <なぜ> |

## 影響範囲（コードベース）

<変更が及ぶモジュール・主要コンポーネント。`/check-plan` で網羅性を確認する対象。関係が複雑なら Mermaid 図を検討（`/design-doc-mermaid` を使用）。>

## タスク一覧（必須）

<実装完了に必要なタスク。実装者は1つずつ `gh issue edit` でチェックを更新する（CLAUDE.md 厳守ルール）。設計書更新が必要なら、その更新タスクを必ず含める。>

- [ ] タスク1
- [ ] タスク2
- [ ] 設計書（〇〇）の更新 ← 設計書への影響が「更新不要」でない限り必須

## 実装フロー（使用するSkill・必須）

<このPlanの変更種別に対応する開発フローSkillを実行順に書く。`/to-issues` が各Issueへ転記し、**issue番号だけで開発フローを再現できる**ようにする。種別→Skillの対応は CLAUDE.md「開発フロー」を正典（SSOT）とし、該当しない種別の行は削除する。>

| 順  | 変更種別                 | 使用Skill   |
| --- | ------------------------ | ----------- |
| 1   | 設計書／ドキュメント更新 | `/design`   |
| 2   | アプリ実装               | `/code-dev` |
| 3   | CDK／インフラ実装        | `/cdk-dev`  |

## オープンクエスチョン

<未解決事項・要確認事項。無ければ「なし」と明記。>
