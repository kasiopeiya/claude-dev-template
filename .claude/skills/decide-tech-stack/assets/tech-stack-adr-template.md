---
# status / date は adr-index.md 生成の SSOT（本文には転記しない）。
# status 許容値: proposed / accepted / rejected / deprecated / superseded
# superseded のときは supersededBy に置換先の3桁番号を書く（例: supersededBy: 003）
status: proposed
date: YYYY-MM-DD
---

<!-- 技術スタックADR専用テンプレート（/decide-tech-stack の出力）。
     単一の決定を記録する通常の ADR は docs/adr/adr-template.md を使う。
     <> は埋めて消す。この注記を含む HTML コメントは記入指示なので、書き上げたら全部消す。
     可視化ファースト（documentation-policy.md 準拠）：比較・制約は表で書き、散文で構造を書かない。 -->

# ADR-NNN: <このプロジェクトの初期技術スタックを決める>

> [!IMPORTANT]
> **TL;DR（この決定の要点）**
>
> - <何を基盤として選んだか。最大3行>
> - <何をあえて保留したか。1行>

## 前提

<!-- 技術選定を「実際に縛った」要件だけを書く。縛らなかった要件は載せない。
     各行の No は、決定表で「与件」と判定した項目の根拠として引かれる。 -->

| No  | 要件・制約   | 出所                                                     | 何を縛るか                       |
| --- | ------------ | -------------------------------------------------------- | -------------------------------- |
| P1  | <要件の内容> | <requirements.md の見出し／既存 ADR／組織標準のいずれか> | <選択肢が狭まる技術項目を名指し> |

## 決定した技術スタック

各項目の「区分」は、次の**外形的な事実**で決まる。

| 区分           | 判定する事実                                                             | 根拠に書くこと                                                             |
| -------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| 与件           | 人間に選択肢を提示していない。かつ要件・既存 ADR・組織標準の出所を引ける | 前提テーブルの No                                                          |
| 実質的な選択   | 複数の候補を人間に提示し、人間が選んだ                                   | 詳細節へのリンク                                                           |
| デファクト踏襲 | 人間に選択肢を提示しておらず、出所も引けない                             | 「他候補を検討していない」                                                 |
| —              | 今回は決めなかった（選定欄が「未使用」または「保留」）                   | 保留なら「意図的に保留したもの」節へのリンク、未使用なら使わない理由を一言 |

<!-- 行は `../references/foundational-decisions.md` の基盤チェックリストの全項目を持つ。
     決めなかった項目も行を消さず、選定欄に次のどちらかを書く（行の欠落＝抜け漏れとして見えるようにするため）。
       未使用＝この構成では該当する技術を使わない（例：DBを持たない）
       保留＝後で決める。「意図的に保留したもの」にも行を立てる
     チェックリストに無い項目を決めたら、行を追加して同じ形式で書く。 -->

| No  | 項目               | 選定   | 区分           | 根拠                        |
| --- | ------------------ | ------ | -------------- | --------------------------- |
| 1   | 言語 / ランタイム  | <選定> | 与件           | P1                          |
| 2   | クラウドプロバイダ | <選定> | 実質的な選択   | [詳細](#クラウドプロバイダ) |
| 3   | <項目>             | <選定> | デファクト踏襲 | 他候補を検討していない      |

## 各項目の詳細

<!-- 「実質的な選択」と判定した項目だけ見出しを立てる。
     与件・デファクト踏襲の項目はここに書かない（決定表の行で完結する）。 -->

### <項目名>

<!-- 全案に利点と代償の両方を書く。採用した案の代償欄を空欄・「なし」にしない。
     読み手が知りたいのは「なぜこれか」だけでなく「これを選んだ結果、何を我慢しているか」だから。 -->

| 案    | 概要             | 利点         | 代償・欠点         | 採否 |
| ----- | ---------------- | ------------ | ------------------ | ---- |
| <案A> | <どんな選択肢か> | <何が良いか> | <何を我慢するか>   | 採用 |
| <案B> | <どんな選択肢か> | <何が良いか> | <何が引っかかるか> | 却下 |

**決め手**：<何を優先し、何を捨てたか。一文で言い切る>

<!-- 構成の前後比較・順序依存・状態遷移があれば /design-doc-mermaid で図を追加する（自前で Mermaid を書かない）。
     表・箇条書きで一目で掴めるなら図にしない＝逆変換テスト。 -->

## 受け入れるリスク・制約

<!-- ここに書くのは、スタック全体として今後の作業に効く制約（項目横断）。
     個々の案を選んだ結果の欠点は詳細節の「代償・欠点」欄が持つので、両方に書かない。 -->

| 受け入れる制約・リスク | 影響範囲       | 緩和策                           |
| ---------------------- | -------------- | -------------------------------- |
| <制約・リスク>         | <どこに効くか> | <どう抑えるか。なければ「なし」> |

## 意図的に保留したもの

足場を組むのに不要で、最初の曳光弾ではスタブで代用できる詳細。「未決定」ではなく**戦略的な保留**である。

| 保留項目 | いま決めない理由                 | いつ決めるか                             |
| -------- | -------------------------------- | ---------------------------------------- |
| <項目>   | <今決めると何を固定してしまうか> | <どの曳光弾／何が分かった時点で決めるか> |
