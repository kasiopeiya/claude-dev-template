---
hook:
  applies-to: ['**/*.test.ts', '**/*.test.tsx', '**/*.test.mjs']
---

# 単体テスト方針

AIエージェントが、テストコードを実装・レビューし、何をどこまでテストするか・テストダブルの使い方に迷ったときに参照する指針。AAA・命名・SUT・テストケースの分離・パラメータ化・フィクスチャの**書き方（How）**は [unit-test.md](../../.claude/rules/unit-test.md)（Rule）が定める。

> [!IMPORTANT]
> **TL;DR**
>
> - 古典学派スタイルを採用する。隔離対象はテストケース、テストダブルはプロセス外依存と、実行を遅く・非決定的にする依存にのみ使用可。
> - テスト対象は一律ルールで決める。アプリ実装とCDK実装だけが対象で、補助スクリプトなどそれ以外は書かない（例外は名指しされたものだけ）。
> - 単体テストでは原則モックを使用しない（例外は[インタラクションテストが適切な場合](#インタラクションテストが適切な場合)に挙げた場面だけ）。スタブも可能な限り避ける。

## 基本思想

### コードは資産ではなく負債である

これは「YAGNI——必要になるまで作らない」（[refined-engineer-judgment-principles](refined-engineer-judgment-principles.md)）そのものである。テストコードも例外なく保守対象。したがって、**明確な価値をもたらさないテストは作成しない**。質の悪いテストを書くくらいなら、書かないほうがマシ。テストスイートの質は、各テストケースを1つずつ評価してしか測れない。何をテスト対象にするかは[テスト対象の判断基準](#テスト対象の判断基準)に従う。

### 古典学派（Classical School）スタイルの採用

- 隔離対象は**コードではなくテストケース**。各テストケースが互いに影響を与えず独立して実行できることを重視する
- テストダブルの使用は**プロセス外依存**と、**速度・決定性を壊す依存**（システム時刻・乱数など）に限定する（[テストダブルの使用方針](#テストダブルの使用方針)）
- 「単体」は1つのクラスに限定せず、**同じ振る舞いを達成するクラスのグループ**も含む

## 検証原則

### 振る舞いを検証し、実装の詳細を検証しない

テストは**観察可能な振る舞い（出力・戻り値・状態変化）**のみを検証する。内部の実装詳細（privateメソッドの呼び出し順序、内部変数の中間状態など）を検証してはならない。

<details>
<summary>なぜ実装の詳細を検証してはならないのか</summary>

実装の詳細を検証すると、振る舞いを変えないリファクタリング（メソッド名の変更、内部処理の再構成など）でテストが壊れる。これは**偽陽性**を引き起こす。偽陽性が多いテストスイートは開発者の信頼を失い、テスト失敗が日常的に無視される（**オオカミ少年問題**）。結果としてテストスイートの存在意義が失われ、本物のバグを見逃す原因になる。
</details>

```typescript
// ✅ 良い例：観察可能な振る舞い（出力）を検証
// → 内部でどのような関数を使っていても、このテストは壊れない
const result = await sut(event, context)
expect(result.statusCode).toBe(200)
expect(JSON.parse(result.body).userId).toBe('user-123')

// ❌ 悪い例：実装の詳細（内部メソッドの呼び出し過程）を検証
// → formatName を rename しただけでテストが壊れる（偽陽性）
expect(formatNameSpy).toHaveBeenCalledWith('alice')
expect(validateInputSpy).toHaveBeenCalledTimes(1)
```

### グレーボックステストで書く

テストケースの**設計と実装**はブラックボックスで行い、テストケースの**過不足確認**にはホワイトボックス的な分析（カバレッジ計測等）を活用する。この2つのアプローチを組み合わせた**グレーボックステスト**を採用する。

| フェーズ                 | アプローチ       | 具体的な活動                                                     |
| ------------------------ | ---------------- | ---------------------------------------------------------------- |
| テストケースの設計       | ブラックボックス | 仕様・振る舞いから必要なテストケースを洗い出す                   |
| テストの実装             | ブラックボックス | 入力と出力のみに着目し、実装の詳細を検証しない                   |
| テストケースの過不足確認 | ホワイトボックス | カバレッジを計測し、未検証の経路からテストケースの漏れを特定する |

**本質的に重要な原則：** 必要なテストだけをやる。ブラックボックスの思想は「実装の詳細を検証するな」の意味であり、「実装を見るな」「テストケースを追加するな」の意味ではない。カバレッジ分析で見つかった未検証の経路が[テスト対象の判断基準](#テスト対象の判断基準)に当たるなら、テストケースを追加する。

<details>
<summary>なぜグレーボックステストを採用するのか</summary>

純粋なブラックボックステストは、仕様からのみテストケースを導出するため、実装上の重要な分岐を見落とすリスクがある。一方、純粋なホワイトボックステストは実装の詳細に依存するため、リファクタリングへの耐性が低い。

本プロジェクトでは両者の長所を組み合わせる：

- **リファクタリングへの耐性**を確保するため、テストの実装（assertの対象）はブラックボックスで行う
- **テストケースの網羅性**を確保するため、カバレッジ計測によるホワイトボックス分析でテストケースの過不足を確認する

つまり「何を検証するか」はブラックボックスで決め、「テストケースが足りているか」はホワイトボックスで確認する。
</details>

### 検証手法の優先順位

| 優先度 | 手法                   | 検証内容             | 使用場面                 |
| ------ | ---------------------- | -------------------- | ------------------------ |
| 1      | 出力値ベース           | 戻り値の確認         | 副作用のない処理         |
| 2      | 状態ベース             | 処理後の状態確認     | 状態を変化させる処理     |
| 3      | インタラクションベース | 依存への呼び出し確認 | 単体テストでは原則不使用 |

## テストダブルの使用方針

### 用語の定義

テストダブル（dummy/stub/spy/mock/fake）の厳密な定義は [test-terms.md](../reference/test-terms.md) を参照（単一の真実）。ここでは単体テストで頻出する**スタブとモックの使い分け**だけを再掲する。

> **判断基準：** そのテストダブルに対して `toHaveBeenCalledWith` 等の**検証（assert）を行うかどうか**で区別する。検証しない → スタブ（データ提供）、検証する → モック（呼び出し検証）。

### 基本方針：単体テストではモック・スタブを可能な限り使用しない

置き換えてよいのは、プロセス外依存と、速度・決定性を壊す依存だけである。内部のクラス・関数・値オブジェクトは本物を使う——テストダブルに置き換えるほど、テストは実装の詳細に縛られ、退行を見つける力を失うからだ。

<details>
<summary>なぜテストダブルの使用を避けるのか</summary>

テストダブルを多用すると、テストが実装の詳細（どのメソッドがどの引数で呼ばれたか）に依存しやすくなり、リファクタリングへの耐性が低下する。また、テストダブルで置き換えた部分は実際のコードが動いていないため、退行（バグ）を検出する能力も下がる。古典学派の原則に基づき、ビジネスロジック（ドメイン）とプロセス外依存の連携（コントローラー）が分離されていれば、ドメインは外部依存を持たないはずであり、テストダブルは不要になる。
</details>

| 依存の種別               | テストダブル使用 | 具体例                        |
| ------------------------ | ---------------- | ----------------------------- |
| プロセス外依存           | ✅ 使用する      | DB、ファイルシステム、外部API |
| 速度・決定性を壊す依存   | ✅ 使用する      | システム時刻、乱数、重い計算  |
| プライベート依存（可変） | ❌ 使用しない    | 内部で利用するクラス・関数    |
| プライベート依存（不変） | ❌ 使用しない    | 値オブジェクト、定数          |

### 速度・決定性を壊す依存も置き換え対象とする

単体テストは**速い**ことと**決定的**であることが絶対条件。プロセス外依存でなくても、この2条件を壊す依存はテストダブルで置き換える。

| 軸       | なぜ置き換えるか                                                   | 具体例                     |
| -------- | ------------------------------------------------------------------ | -------------------------- |
| 実行時間 | 実行に時間のかかる依存はテスト全体を遅くし、頻繁な実行を妨げる     | 重い計算、ネットワーク往復 |
| 決定性   | 実行ごとに結果が変わる依存は偽陽性を生み、テストへの不信につながる | システム時刻、乱数         |

### スタブとのやりとりを検証してはならない

スタブは最終的な結果を生成するために必要なデータを提供するだけのもの。

<details>
<summary>なぜスタブとのやりとりを検証してはならないのか</summary>

スタブの呼び出し過程（どの引数で呼ばれたか、何回呼ばれたか）を検証することは**過剰検証（over specification）**にあたる。これはテストを実装の詳細に結びつけることになり、内部のリファクタリングでテストが壊れる原因になる。検証すべきはスタブから得たデータを使って生成される**最終的な結果のみ**。
</details>

### 外部通信の検証は単体テストではなく統合テストで行う

単体／統合の境界は「本物のプロセス外依存に実際に触れるか」で決まる。プロセス外依存（外部API等）をテストダブルで差し替えれば本物に触れていないので、外部依存への**呼び出し自体の検証（モック）**も技術的には単体テストとして書ける。

ただし本プロジェクトでは、**外部通信の検証は単体テストで行わず統合テストに委ねる**。統合テストで本物の依存を使うか境界でダブル化するかは依存の制御可否によって決める（[test-strategy-policy.md](test-strategy-policy.md) の「制御できる依存は本物、できない依存は境界でダブル化」）。単体テストでモックによる呼び出し検証を行うのは、次項に挙げた場面に限る。

<details>
<summary>なぜ単体テストでのモック検証を基本にしないのか</summary>

モックによる呼び出し検証は実装の詳細（どの依存をどう呼んだか）に結びつきやすく、退行（バグ）の検出能力も下がる。連携が「本当に動くか」は本物に近い依存を使う統合テストで初めて保証できるため、単体テストでのモック検証は、呼び出しの回数・順序自体が仕様である場合と、本物もフェイクも用意できない暫定策に限る。
</details>

### インタラクションテストが適切な場合

原則は **「インタラクションテストよりステートテストを優先する」**。状態や戻り値で振る舞いを検証できるなら、そちらを使う。ただし次の場合に限り、呼び出し自体を検証するインタラクションテストを使ってよい。

| 場面                                 | なぜインタラクションテストになるか                                         | 例                                          |
| ------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------- |
| 本物の実装もフェイクも使えない       | 状態で検証したくても、検証に使う本物・フェイクが用意できない               | 他チームが開発中で間に合わない依存          |
| 呼び出しの回数・順序自体が仕様である | 回数や順序を誤ること自体が望ましくない挙動であり、状態だけでは検証できない | キャッシュ（2回目は元データを呼ばない、等） |

<details>
<summary>各場面の補足</summary>

- **本物もフェイクも使えない：** 例えば、外部依存を別チームが開発中でリリースが遅れ間に合わない、依存の仕様が未確定でフェイクすら作れない、といったケースが該当する。あくまで暫定策であり、本来は統合テストで本物を使って検証するのが望ましい。依存が使えるようになった時点でそちらへ寄せる。
- **回数・順序が仕様：** キャッシュのように「1回しか呼ばないこと」自体が守るべき振る舞いの場合、最終状態が同じでも内部の呼び出し回数を検証しないと仕様を保証できない。この場合の呼び出し検証は実装の詳細ではなく観察可能な振る舞いの検証にあたる。

</details>

## テストコードの書き方

### テストはDRYよりDAMPであるべき

テストコードは、多少の重複を許容してでも **DAMP（Descriptive And Meaningful Phrases：説明的で意味のわかる記述）** を優先する。各テストケースが、他の箇所を読みに行かなくても単体で「何を・どんな条件で検証しているか」を語れる状態を目指す。

<details>
<summary>なぜテストではDRYよりDAMPを優先するのか</summary>

- **テストは「失敗したときに読まれる」もの：** テストが読まれる主な場面は失敗時であり、そこで素早く原因を把握できることが最重要
- **共通ヘルパーへの過度な抽象化は、読む箇所を増やす：** Act/Assert のロジックを共通ヘルパーに隠すと、テスト失敗時に「テスト本体」と「ヘルパーの中身」の両方を行き来して読む必要が生じ、原因特定が遅れる
- **各テストが独立して意図を伝えられると、デバッグコストが下がる：** テストケース単体で前提・操作・期待結果が完結していれば、他のテストやヘルパーの文脈を読み込まずに失敗を理解できる

</details>

> [!IMPORTANT]
> **（AI・必須）** **DRYがダメというわけではない。** DRYによるリファクタリングは、あくまでテストが説明的で意味がわかりやすいことを重視した上で行うべきであり、単に繰り返しを減らすという目的だけで行ってはならない。

なお、テストデータ生成のファクトリ（[テストデータの共通化パターン](../../.claude/rules/unit-test.md#テストデータの共通化パターン)）は DAMP と矛盾しない。`createUser({ membershipLevel: 'gold' })` のように **入力データの意図が呼び出し側に残る**抽象化は説明性を高める。DAMP が避けたいのは、**Act（実行）や Assert（検証）のロジックそのものをヘルパーに隠してしまう**抽象化。

```typescript
// ❌ 悪い例：DRYを優先しすぎ、テスト単体では何を検証しているか読み取れない
it('割引が適用される', async () => {
  const result = await runDiscountTest('gold', [1000, 2000])
  expect(result).toBe(2700)
})
// → runDiscountTest を読みに行かないと、割引率も会員種別の意味も、なぜ2700なのかもわからない
//   失敗時に「テスト本体」と「ヘルパー」の往復が発生する

// ✅ 良い例：DAMP。多少の重複はあるが、テスト単体で前提・操作・期待結果が完結している
it('ゴールド会員は注文金額が10%割引される', async () => {
  const user = createUser({ membershipLevel: 'gold' }) // データ生成の抽象化は意図が残る
  const event = createOrderEvent({ userId: user.id, items: [1000, 2000] })

  const result = await sut(event, context)

  expect(JSON.parse(result.body).totalAmount).toBe(2700) // 3000 の 10%割引
})
```

## カバレッジの考え方

### カバレッジの位置づけ

カバレッジはテストケースの**過不足を確認するための分析ツール**として積極的に活用する。

### 運用方針

- カバレッジ率の**目標値の強要はしない**（100%でなければコミットできない等は不適切、Goodhartの法則）
- カバレッジ計測で未検証の経路が見つかった場合、その経路が[テスト対象の判断基準](#テスト対象の判断基準)に該当するなら**テストケースを追加する**
- カバレッジの数値そのものではなく、**未検証の経路の中身**を見て判断する

## テスト対象の判断基準

「コードは資産ではなく負債である」の原則に従い、テスト対象は**一律ルール**で決める。

### テスト対象

- 単体テスト：アプリ実装のコア（ドメイン・サービス・コントローラー等）、ドメインの不変条件を守る事前条件チェック（ガード句）、サポートログ（外部向け：いつ・誰が・どの操作をしたか）の出力
- snapshotテストのみ（fine-grainedテストは任意）：CDK実装

フロントエンドもアプリ実装に含む。何をどう検証するかは[フロントエンド固有のガイドライン](#フロントエンド固有のガイドライン)が定める。

<details>
<summary>テスト対象とする理由</summary>

- **コア実装：** テストの費用対効果が最も高い部分。バグが混入した場合のビジネスへの影響が大きく、テストによる保護の価値が高い
- **事前条件チェック：** データの整合性を守る不変条件の侵害を防ぐ処理であり、壊れた場合の影響範囲が広い
- **サポートログ：** カスタマーサポートや監査など外部から観察されることを意図したログは、アプリケーションの観察可能な振る舞いの一部
- **CDKはsnapshotのみ：** 目的は意図しないテンプレート差分の検知であり、1本で全リソースを覆える。個別リソースのfine-grainedテストはCDKの宣言をなぞるだけになりやすいため任意とする

</details>

### 異常系テストの基準点

異常系テストが足りているかは、**設計書の記載ではなく実装コードの分岐**を基準に判断する。設計書は書き漏れうるが、実装コードは実在する。

- アプリ実装のテスト対象コードの異常系分岐（`throw`・エラーを表す戻り値・ガード句・バリデーション）には、設計書に記載が無くてもテストを書く
- 分岐の条件式が大小比較・長さ・範囲を含むなら、境界の両側をテストする

書かない場合は[テスト対象外](#テスト対象外)に名指しされたものだけである。

### テスト対象外

- **アプリ実装とCDK実装以外のすべて**：補助スクリプト（`scripts/`・`eslint-rules/`・`.claude/hooks/` 等）、テスト用コード
- **アプリ実装の内側**：privateメソッド・private関数、単純な委譲のみのコード、診断ログ（開発者向け）の出力
- **異常系分岐のうち次の3種**：正常な呼び出し経路から到達しない防御的分岐、型システムが既に排除している入力のチェック、受け取った例外に情報を足さず再 `throw` するだけの `catch`
- **自分が書いていないコード**：ライブラリ内部の挙動

<details>
<summary>テスト対象としない理由</summary>

- **補助スクリプト：** 壊れても影響は開発者の手元に留まり、実行のたびに人間が結果を目にする。テストの保守コストに見合わない
- **テスト用コード：** テストヘルパーやスタブ実装が壊れればテスト自体が落ちるため、別のテストで守る必要がない
- **privateメソッド・private関数：** 実装の詳細にあたる。直接テストするとリファクタリング時にテストが壊れ（偽陽性）、リファクタリングへの耐性を損ねる。privateな処理はそれを呼び出すpublicなAPIを通じて間接的に検証されるべき
- **単純な委譲のみのコード：** テストを書いても検出できるバグがほぼなく、費用対効果が低い
- **診断ログ：** 開発者しか見ないログは実装の詳細であり、外部から観察される振る舞いではない。テスト対象にすると内部構造の変更でテストが壊れる
- **ライブラリ内部の挙動：** サードパーティの責務であり、自プロジェクトのテストで保証すべき範囲ではない

</details>

### 例外一覧（対象外の区分に入るがテストを書くもの）

- `.claude/hooks/policy-loader.mjs`：ポリシー駆動開発の土台。壊れるとポリシーがAIに届かず、全ポリシーが無言で効かなくなる
- `.claude/hooks/policyMatcher.mjs`：同上（対象ファイルとポリシーの対応判定を担う）
- `scripts/auto-programmer/` のうち、import 文を1つも持たないファイル：無人で走るツールの判断（どの Issue に着手し、どのブランチに積むか）を担う純粋関数である。壊れると誤った PR が、誰にも見られずにできる
- **fake**：それ自体が独自のロジックを持ち、壊れると依存する全テストが嘘の結果を返す

> [!IMPORTANT]
> **（AI・必須）** 例外はこの一覧に**名指しされたものだけ**。「これも重要な仕組みだから」という理由で自分で例外を増やしてはならない。増やしたい場合は人間に確認する。

## フロントエンド固有のガイドライン

### フロントエンドの基本方針

フロントエンドの単体テストも古典学派の原則に従い、**ユーザー操作に対する振る舞い**を検証する。内部の実装詳細（state変数の値、内部メソッドの呼び出し）ではなく、ユーザーが観察できる結果（画面の表示内容、DOM の変化）を検証する。

### フロントエンドの検証対象

- **コンポーネント**：ユーザー操作に対するレンダリング結果
- **カスタムフック**：入力に対する戻り値・状態変化
- **ユーティリティ関数**：入力に対する出力値

### コンポーネントテストの原則

- **ユーザーの視点**でテストする（Testing Libraryの思想に準拠）
- 要素の取得は**ユーザーが認識できる属性**で行う（具体的なクエリの優先順位は [react.md](../../.claude/rules/react.md) が定める）
- 内部stateやpropsの直接検証はしない

```typescript
// ✅ 良い例：ユーザー視点で振る舞いを検証
it('フォーム送信失敗時にエラーメッセージを表示する', async () => {
  render(<LoginForm />)

  await userEvent.click(screen.getByRole('button', { name: '送信' }))

  expect(screen.getByText('入力内容を確認してください')).toBeInTheDocument()
})

// ❌ 悪い例：実装の詳細（state）を検証
it('送信時にエラーstateをセットする', () => {
  const { result } = renderHook(() => useLoginForm())
  act(() => result.current.handleSubmit())
  expect(result.current.errorState).toBe(true)  // 内部stateの検証
})
```
