# 判断ログ（素材層）

**想定シーン**：原則を実データから育てたいAI・人間が、(a) 判断を記録する／(b) 貯まった素材を分析して原則へ昇格させる、とき。

[refined-engineer-judgment-principles.md](policy/refined-engineer-judgment-principles.md) に載せる原則を**実データから抽出する**ための、加工前の素材を貯めるappend-onlyログ。AIが自律的に下した非自明な判断と、人間からの判断矯正を生のまま記録する。これ自体は規範（ポリシー）ではなく、原則を育てるための**観測データ置き場**である。

## なぜ独立した層が必要か（feedbackメモリとの役割分担）

feedbackメモリ（`~/.claude/.../memory/`）は「自明でない・一般化できる事実だけを残す」フィルタを通過した**加工後の成果物**であり、その瞬間に以下を捨てている：

- **棄却された矯正**：些細／一回限りと判断して保存しなかった指摘。だが複数回再発すれば原則化すべき素材だった
- **認識されなかった矯正**：そもそも矯正と気づかなかったもの（最大のブラインドスポット）
- **頻度・再発情報**：重複を統合するため「何回起きたか」が消え、昇格判断ができなくなる
- **生の文脈**：一般化された事実に圧縮され「どの状況で・どんな代替案を却下して」が消える

メモリは原則抽出の**データソースにはならない**（抽出前の素材が残っていないため）。そこでこのログを素材層として置き、メモリ／principles／rules はその**プロモーション先**とする。

```mermaid
flowchart TD
    Correction([👤 correction<br/>人間がAIの判断を矯正]) --> Log
    Decision([⚙️ decision<br/>AIの非自明な自律判断]) --> Log
    Log[(📝 判断ログ<br/>append-only 素材層)] --> Analyze[🔄 分析・クラスタリング]
    Analyze --> Judge{✓ 昇格判定}
    Judge -->|universal × 複数回| Principles[🧭 refined-engineer-<br/>judgment-principles.md]
    Judge -->|project / 技術固有| Rules[📐 .claude/rules<br/>または個別 policy]
    Judge -->|行動指針として常時想起| Memory[💾 feedback メモリ]
    Judge -->|基準未達| Open[⏳ open のままログに留まる]

    classDef event fill:#E6E6FA,stroke:#333,stroke-width:2px,color:darkblue
    classDef store fill:#87CEEB,stroke:#333,stroke-width:2px,color:darkblue
    classDef process fill:#90EE90,stroke:#333,stroke-width:2px,color:darkgreen
    classDef decision fill:#FFD700,stroke:#333,stroke-width:2px,color:black
    classDef dest fill:#90EE90,stroke:#2E7D2E,stroke-width:2px,color:darkgreen
    classDef hold fill:#FFB6C1,stroke:#DC143C,stroke-width:2px,color:black

    class Correction,Decision event
    class Log store
    class Analyze process
    class Judge decision
    class Principles,Rules,Memory dest
    class Open hold
```

## 何を記録するか（トリアージ）

価値の高い順に：

1. **correction（最優先）**：人間がAIの判断・実装を矯正した瞬間。判断ギャップが外部からラベリングされた金の教師データ
2. **decision**：AIが自律的に下した非自明な設計判断・選択（トレードオフを伴うもの）

**記録しないもの**：自明で正しかった判断、会話限りの瑣末な選択。素材層とはいえノイズは薄める。

## 記録タイミング

CLAUDE.md「AIへの基本指示」の*やり取りで得た知見を回答末尾にまとめる*習慣と連動させる。具体的には、その回で **(a) 人間に判断を矯正された**、または **(b) トレードオフを伴う非自明な自律判断を下した** 場合、回答末尾でその旨に触れ、本ファイルへ1エントリ追記する。

## エントリ・テンプレート

各エントリは、ヘッダーの **話題**（何についての判断か。短い名詞句）＋本文の3項目で捕捉する。本文は `AI判断`（AIが下した判断・実装）、`人間指摘`（人間が示した修正方向）、`原因`（なぜAIの判断がズレたか＝失敗モード）。狙いは「AIに判断を任せる基準づくり」で、その基準とは結局**AIが繰り返す失敗モードのカタログ**。だから捕捉すべき生信号は各事例の `原因` であり、`話題` はその原因が**話題固有か話題横断か**を見分ける軸になる（→ 同じ失敗が一話題に偏れば project 固有ルール、横断で出れば universal 原則、という昇格の振り分けに効く）。

一般化した「教訓・原則」は**捕捉時に書かない**。1件だけで一般化すると粒度を外す（本ファイルの「加工は分析時に後回し」に反する）。教訓は複数の `原因` を見比べて分析時に立ち上げる産物であり、スコープ・ステータス同様、捕捉時には持たせない。

```markdown
- YYYY-MM-DD [correction] ＜話題：何についての判断か。短い名詞句＞
  - AI判断：
  - 人間指摘：
  - 原因：なぜそう誤ったか（失敗モード）
```

[decision]（人間の矯正がない自律判断）の場合は、`人間指摘`→`却下案`（採らなかった対立案）、`原因`→`狙い`（なぜそう決めたか）に読み替える。

<details>
<summary>記入例（フォーマット説明用の仮想エントリ）</summary>

```markdown
- 2026-06-14 [correction] CDK の物理名命名
  - AI判断：S3物理名を `my-app-${env}` で明示指定しようとした
  - 人間指摘：env取り違えで diff 誤検知になる。PhysicalName.GENERATE_IF_NEEDED に委ねよ
  - 原因：明示命名を「分かりやすさ」と捉え、決定論性・diff影響への波及を見落とした
```

</details>

## プロモーション（昇格）フロー

定期的（月次 or エントリが一定数たまったとき）に、前回レビュー日以降のエントリを見直す：

1. **クラスタリング**：似た原因（失敗モード）をまとめ、再発回数を数える
2. **昇格判定**：
   - `universal × 同種2件以上`（または明確な普遍性）→ principles へ昇格
   - `project/技術固有` → rules または個別 policy へ随時反映
   - 行動指針として常時想起したい → feedbackメモリへ反映

昇格の記録は**昇格先（principles 等）に残す**。ログ側は生のまま蓄積し、個々のエントリにステータスは持たせない（どこまで処理したかは「前回レビュー日」で管理する）。

> [!IMPORTANT]
> **（AI・必須）** principles は「判断の北極星」であり肥大化させない（原則4・5）。N=1の事象を性急に昇格させない。固有・瑣末なものは principles に入れず rules / 個別 policy に振り分ける。

---

- 2026-07-02 [decision] 空ファイルの割れ窓を起票するか
  - AI判断：`cdk-design-policy.md`→`cdk-policy.md` リネーム作業中、リネーム対象が中身空（0バイト）のまま2箇所から参照されている状態を発見。boy-scoutルールに従い起票可否をユーザーに確認した
  - 却下案：確認なしにそのままIssue起票（CLAUDE.mdのboy-scout運用ルールは「確認なしに起票」と明記されている）
  - 狙い：リネーム自体は依頼範囲内の軽微な作業だが、「中身のない空ポリシーファイルが参照され続けている」は放置期間・原因が不明で、単純な割れ窓と言い切れるか自信がなかったため、確認なし起票の既定運用よりも安全側（AskUserQuestion）に倒した。ユーザーは「起票しない」を選択

---

## ログ

<!-- 新しいエントリを上に追記（逆時系列） -->

- 2026-07-04 [correction] 質問/指示の判別ルールを対称的な動詞分類で設計し、非対称コストを踏まえた片側リストの方が効率的という指摘を受けた
  - AI判断：「質問には回答で、指示には作業で応える」ルールを、相談動詞（考えて／思う／教えて）と指示動詞（して／直して）の**両方を列挙して分類する**形で設計・改訂を重ねた
  - 人間指摘：明確な作業指示（対応開始して／作業して等）があるかどうかだけを判定し、それ以外は全て相談として扱う片側リスト＋デフォルトの方が情報量が半分で済む
  - 原因：相談と指示を対称な二値分類問題と捉え、両側のリストを充実させることで精度を上げようとした。しかし実際は誤判定のコストが非対称（相談を指示と誤読＝勝手に編集、の方が実害が大きい）なので、片側だけリスト化し残りをデフォルトで安全側に倒す設計の方が、リスト漏れにも強く保守コストも低い。対称性に引きずられ、コストの非対称性から設計を組み立て直す発想が抜けていた

- 2026-07-04 [correction] 「改善策を考えて」を編集指示と誤認し、追加したばかりの確認ルールを自分で破った
  - AI判断：ユーザーの「CLAUDE.mdの動作不満2点について改善策を考えて」を受け、改善案をCLAUDE.mdに直接Editで反映した（提案・確認は挟まなかった）
  - 人間指摘：「なんで作業しちゃったの？確認だったのに」。しかも追加した内容がまさに「質問には回答で、指示には作業で応える」ルールで、その場で自分の追加ルールを破った
  - 原因：文の後半「CLAUDE.mdにシンプルな記述を追加することでこれらを改善したい」という願望の記述を、動詞「追加して」と同一視してしまった。文全体の主動詞は「考えて（＝相談）」であり、後半は相談の中で触れた解決の方向性にすぎず、着手許可ではなかった。指示か相談かの判別を、文中に出た手段の名詞（CLAUDE.md・追加）だけで済ませ、文全体の主動詞に立ち返らなかったのが失敗モード。関連メモリ候補：作業着手前は主動詞（考えて/教えて vs して/直して）で判別する

- 2026-07-04 [correction] 「SLO を設計に組み込む」を狭い SLO 予算チェックに矮小化し、requirements.md の配線という本筋を外した
  - AI判断：ユーザーの相談（SLO は設計、特にインフラ設計で重要では）を「SLO 予算適合の条件付きチェック」と解釈し、check-plan に観点 H（SLI に効く変更のみ発火・レイテンシ予算配分の照合）と plan-template に SLO 専用節を新設した。SSOT の置き場所（新規ポリシー vs check-plan）を主論点に据えた
  - 人間指摘：本筋は requirements.md（＝SLO を含む要件の正典）を**設計フローの一級市民**として3点で配線すること——①grill-me/plan で参照させる ②issue 化時に requirements.md 更新タスクを含める（必要時のみ）③/design 完了後に requirements.md 充足をチェックする。requirements.md は存在前提で書く（「未作成なら」の degrade を書かない）
  - 原因：要素（SLO 予算）に飛びつき、それを運ぶ器（requirements.md を参照・更新・検証する仕組み）を見落とした。①既存の「設計書への影響／更新タスク／validate 系」機構に requirements.md を並べれば済む所を、SLO 専用の新機構を発明した（原則5・車輪の再発明）②「存在しないかも」と degrade を書き、確定した前提を勝手に不確実化した③発散を1軸（SLO 適合）に絞り、要件全体の参照・更新・充足検証という広い本筋を捕捉できなかった。関連メモリ [[feedback_avoid_overcorrecting_to_feedback]] [[feedback_review_rules_must_be_in_scoring_spine]]

- 2026-07-04 [correction] SLO 逸脱アラートの必須化を「症状ベース SLI」に限定し、原因ベース SLI を候補どまりにした（SLO 逸脱の見逃しを生む切り分けだった）
  - AI判断：「SLI 基準割れをアラート必須にしてよいか、ただし症状ベースだけを対象にすべきか」というユーザーの問いに乗り、必須化対象を症状ベース SLI に限定・原因ベース SLI は候補どまりとした。オオカミ少年（原因系の無差別ページ化）を防ぐ線引きだと考え、elicit-requirements の SLI 表に「種別」列まで足した
  - 人間指摘：SLI＝具体的な計測対象、SLO＝その目標値。SLI（SLO）はすべてアラート対象にすべき。原因ベースを候補どまりにすると、コミットした SLO を割っても能動通知されず見逃す
  - 原因：オオカミ少年の防御線を取り違えた。ノイズを防ぐのは「SLO 逸脱アラートを種別で絞る」ことではなく、① 原因系メトリクス（CPU・キュー長）はそもそも SLI にしない ② 瞬間値でなく SLO／エラーバジェットのバーンレートで鳴らす、の2点。SLO は元々少数かつ全てユーザー影響なので全て鳴らしてよい——「SLI（瞬間値）で全部ページ」への警戒を「SLO 逸脱アラートの絞り込み」に誤って適用し、コミット済み目標の見逃しという本末転倒を作った。ユーザーが問いに混ぜた前提（症状ベースだけ？）を検証せず追認した点も、直前の [correction]（意見を求める問いを指示と解釈）と同型。関連メモリ [[feedback_avoid_overcorrecting_to_feedback]] [[feedback_no_process_notes_in_docs]]

- 2026-07-03 [decision] elicit-requirements の SLI/SLO で「要件に入れる範囲」を評価期間まで広げ、集計機構は実装へ切り出した（人間が「入れるべきものは必ず入れる」と方針指示・境界線は AI）
  - AI判断：ユーザーの質問（集計方法・集計期間は要件定義に書くか）に対し、「集計期間」が多義——SLO評価期間（コンプライアンスウィンドウ）と、メトリクス解像度・ロールアップ——である点を分離。目標値と不可分な評価期間は要件（目標値だけでは分母が未定義）、ロールアップ・累積・統計種別・解像度は計装＝実装（monitoring-policy L35-36）と線引きし、SKILL.md の SLO 定義を「目標値＋評価期間」必須へ改訂し include/exclude を表で明示。カウント/タイムベースの別は指標の意味を変えるので計算式の一部＝要件と較正。最初 `[!NOTE]` callout で境界を書いたが documentation-policy hook を受け、2分類は表がデフォルト（§196）・行動を促す callout はラベル要（§259）に反すると自己判定し、散文＋表へ差し替えた
  - 却下案：集計方法まで全部要件に書く（実装詳細の前倒し固定＝原則2）／評価期間を書かず目標値だけにする（SLO が定義として不完全）／境界を NOTE callout のまま残す（分類を散文化・ラベル漏れで doc-policy 違反）
  - 狙い：要件は「何を測るか（計算式・ソース）」「どこまで目標か（目標値・評価期間）」まで、「どう積むか」は実装、と WHAT/HOW で線を引く。多義語（集計期間）は片方に倒さず分解してから振り分ける。表化は「短いから散文で十分」の誘惑を断つ既定。失敗モード候補：分類を callout/散文で書き、表デフォルトを飛ばす。関連メモリ [[feedback_no_process_notes_in_docs]]

- 2026-07-02 [decision] 要件定義用に新規 Skill `/elicit-requirements` を自作。既存 interview-me の丸ごと導入を退け、grill-me と責務が割れる要求分析専用に設計（人間が「自作が良い」と方針決定・詳細は AI 自律）
  - AI判断：ユーザーは当初「他者製の良い Skill を探して」だったが、調査した Sorbh/interview-me は (1)出力が PRD より設計 spec 寄り (2)Phase 構成が既存 grill-me＋to-plan＋to-issues と三重に責務重複、と判明。ユーザーの真の狙い（RFC の「解決策」を「目的」に還元し、「制約」を疑って requirements.md を作る＝上流の要求分析）は既存フローに無い独立責務のため、丸ごと導入でなく新規 Skill を自作。house style は grill-me（短い思想駆動プロンプト＋refined-engineer-judgment-principles 参照）に合わせ、出力は既存の doc-reviewer-agent が定義する PRD 像（ターゲット/解決する課題/要件）に整合。心臓部を「解決策と目的の分離／制約を疑う／目的を一文で合意」の3姿勢に凝縮。名前は命名規則（責務直結）から `elicit-requirements`、CLAUDE.md 開発フローへの登録は今回見送り（reversible優先・スコープ非拡大）。documentation-policy hook 発火に対しては、SKILL.md は読者向け設計書でなく実行プロンプトのため TL;DR/図表/対象読者行の機械適用は過剰適用と判断し、grill-me 準拠に留めた（[[feedback_avoid_overcorrecting_to_feedback]]）
  - 却下案：interview-me を丸ごと導入（grill-me/to-plan/to-issues と責務二重化。[[feedback_split_policies_over_merge]]／[[feedback_prefer_srp_over_dry_for_docs]]）／grill-me に要求分析を混ぜる（汎用の詰めと上流要求分析の責務が滲む）／documentation-policy の読者向けルール（TL;DR・図表・想定シーン）を SKILL.md に機械適用（実行プロンプトを設計書化して house style を壊す過剰適用）／出力を独自 spec 構造にする（doc-review の PRD 観点と非整合になり後工程が切れる）
  - 狙い：「他者製を探す」依頼でも、実物が目的に対し責務重複・抽象度不一致なら正直に自作へ倒す（CLAUDE.md「正直に振る舞う」）。新 Skill は既存フローに無い独立責務（要求分析）に絞り、出力を既存 PRD 像・後工程（doc-review/grill-me/to-plan）に接続。ユーザー離席中の未確定点（名前・出力先・フロー登録）は原則（命名規則・reversible・スコープ非拡大）から一意に出るものだけ既定で進め、覆せる形に留めた。失敗モード候補：「既存を探して」を額面通り受け、目的に合わない他者製を責務重複のまま導入する／メタツール（SKILL.md）に読者向け文書ポリシーを機械適用する。関連メモリ [[feedback_split_policies_over_merge]] [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-07-02 [decision] 「目的ベースのクラス命名で神クラスを防ぐ」の置き場を、rules/typescript.md ではなく application-design-policy.md 主・rules ポインタ従に分けた（人間が方針追認・タイトル指定）
  - AI判断：ユーザーは当初 typescript.md（命名規則）への追加を想定していたが、Money 神クラス問題の本質は命名規則ではなく SRP/凝集の設計原則だと判断。application-design-policy に既存の「引数は責務を映す鏡である／ブーリアン引数アンチパターン／多重ネストは設計を見直すシグナル」＝すべて SRP を各文脈へ具体化した同型セクション群があり、「神クラスアンチパターンを禁ずる」はその完全な兄弟に当たる。よって根拠（結合・凝集・原則1）を語る本体は policy に `##` 新設し、typescript.md 命名規則には「広い名前を避けよ・根拠は policy 参照」の1行ポインタだけを置いた（ネスト節が typescript.md↔policy で相互参照している前例に倣う）。本体には「名詞か動詞かではない＝Money も値オブジェクトなら正当」の NOTE を付し、§関数とクラスの使い分け の値オブジェクトと接続して過剰一般化を防いだ
  - 却下案：typescript.md だけに命名規則として書く（機械的規約に設計判断の根拠まで載せると rules と policy の責務が混ざる。[[feedback_split_policies_over_merge]]／[[project_policy_docs]]）／policy に本体を書き rules 側は無言（実装時参照の rules に載らず素通り。[[feedback_review_rules_must_be_in_scoring_spine]] と同型）／SRP の定義を policy 本文に再説明（原則1 と二重管理。SSOT 崩し）
  - 狙い：レビュー機構・文書の振り分けは「対象（命名か設計か）」でなく「責務軸（機械的規約か・判断の根拠か）」で決める——命名の戦術は rules、なぜそう命名するかの根拠は policy に一本化し、相互参照で繋ぐ。既存の同型セクション群に兄弟として並べることで一貫性と発見性を確保した。失敗モード候補：ユーザーが指定した置き場（rules）をそのまま受け、設計原則を機械的規約の文書に埋めて責務を割る。関連メモリ [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_split_policies_over_merge]] [[feedback_review_rules_must_be_in_scoring_spine]]

- 2026-07-01 [decision] 5ポリシーを code-review の採点観点に載せる際、固定点数を廃し「得点率(%)＋対象外は分母除外」へ採点モデルを変更した（人間が2択で追認）
  - AI判断：条件付き観点（frontend-design＝フロントのみ／unit-test＝テストのみ）を足すと「固定N項目・M点満点」がファイル種別で変動し、観点追加のたび総点・閾値の書き換えが発生する（直前 [correction] で人間が嫌った churn）。得点率＝合計÷(適用観点数×3) に変え、対象外観点は分母から除外する方式にすれば、観点を増減しても閾値・総点のハードコードを一切触らずに済む。閾値は現行点数比（92/71/51%）から 90/70/50% に丸めた。code-comment-policy は既存 #9 と同一関心のため新項目を作らず #9 を SSOT 参照に補強（fold-don't-add）。unit-test-policy はテストファイルが Phase1 で除外されており永遠に発火しない障害があったため、除外リストから .test/.spec を外し Test 種別を新設
  - 却下案：固定点数を維持し種別別の満点表を持つ（種別ごとに満点が変わり表が増殖・churn 継続）／code-comment を #23 として新設（#9 と二重採点・[[feedback_no_hardcoded_counts_or_enumerations]] に反する）／テスト除外を残す（unit-test-policy が発火せず [[feedback_review_rules_must_be_in_scoring_spine]] の「背骨に載せても素通り」を再現）
  - 狙い：観点の追加コストを「本表に1行足すだけ」に閉じ込め、他ファイル（総点・閾値・report）へ波及させない設計にした。各採点行は SSOT 参照だけにして基準の転記を避け（直前 correction の学び）、実体の照合は agent が各 policy を Read して行う。着手前に (1)code-comment の重複 (2)条件付き項目と固定点数の非整合 (3)テスト除外との衝突 を洗い出し、人間の裁量が要る2点だけ AskUserQuestion に絞った。失敗モード候補：「観点を追加」を鵜呑みにして既存項目との重複・採点モデルとの整合・対象ファイル除外設定を確認せずに行だけ足す。関連メモリ [[feedback_review_rules_must_be_in_scoring_spine]] [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-07-01 [correction] review-criteria #18 にポリシーの具体チェック（①外出し値②機密実値③config境界）を転記した
  - AI判断：#18 のチェック内容欄に configuration-policy の3基準を書き下し、rubric 単体で何を見るか分かる状態にした
  - 人間指摘：policy の中身が増えても review-criteria の記載が増えなければ OK＝rubric に基準を転記するな。ポリシーを SSOT とし #18 は参照だけにせよ
  - 原因：[[feedback_review_rules_must_be_in_scoring_spine]]（背骨に載せる）を「rubric に基準を書き下す」と過剰解釈し、[[feedback_no_hardcoded_counts_or_enumerations]]（転記＝SSOT 二重化で更新コスト）と衝突させた。背骨に必要なのは「項目の存在」であって「基準の転記」ではない——照合の実体は agent が policy を Read して行うので rubric は参照で足りる、と切り分けられなかった。同型の転記回避は 2026-06-28 #160 でも指摘済み

- 2026-07-01 [decision] configuration-policy を code-review に載せる際、fold-don't-add の前例に反して新スコア項目 #18 を新設した（人間が確認で追認）
  - AI判断：新設 configuration-policy を [[feedback_review_rules_must_be_in_scoring_spine]] に従いレビューの背骨に載せる必要があった。過去の #5/#7/#14 は既存項目への fold で対応（新項目は件数 churn を招くとして却下：judgment-log 2026-06-30 #115・2026-06-29 #125）。今回は「外出し判断・機密実値の非埋込・config モジュール境界（process.env 非散在＋fail-fast）」が既存のどの1項目にも自然に畳めない多面的関心のため、独立 #18 が妥当と判断。総点17→18・51→54点、レベル判定閾値（比例再計算）、guide 参照文、report-format、agent 照合セクションの5箇所を一貫更新
  - 却下案：#11「セキュリティ」を「セキュリティ・構成管理」に拡張して畳む（前例踏襲・件数 churn 回避。だが config モジュール境界が security に収まらず foldが不自然）／SKILL.md への参照追記だけで済ませる（[[feedback_review_rules_must_be_in_scoring_spine]]：背骨に載らず素通り）
  - 狙い：fold-don't-add は「畳める関心」に対する既定であって絶対則ではない。畳むと不自然になる多面的関心は独立項目の方が検出力・可読性で勝る、と前例の射程を切り分けた。最終確認 grep で自分が前例に反した点に気づき、勝手に確定させず AskUserQuestion で追認を取った（[[feedback_no_hardcoded_counts_or_enumerations]] の churn コストと検出力のトレードオフを人間に開示）。失敗モード候補：過去 judgment-log の前例を着手前に検索せず、後から矛盾に気づく。関連メモリ [[feedback_review_rules_must_be_in_scoring_spine]] [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-06-30 [decision] SRP 強制策として提案された「全関数の JSDoc に責務を明記」を退け、レビュー観点（一文テスト）＋モジュール冒頭の責務コメントに落とし込んだ
  - AI判断：(1) ユーザー提案「JSDoc に責務を書かせて SRP を守らせる」に対し、コメントは構造的強制力を持たず（責務を書いた隣で平気で複数責務を実装できる）SRP 違反は通るため、全関数必須化は退けた。代わりに価値の核「責務を一文で言えるか／"and" が要れば分割」をレビューの判定軸に昇格させ review-criteria #5 を増補（新行は足さず 51点満点を不変に）。(2) モジュール責務コメントは policy ではなくコード規約 typescript.md のコメント節に新設し（react.md が継承）、「名前で足りるならコメント不要」の例外＝ファイル境界は名前1つで表せないという WHY で既存方針との衝突を回避。(3) ルールに歯を持たせるため責務コメントの有無を #9「コメント」にも織り込んだ
  - 却下案：全関数 JSDoc 必須化（WHAT の直訳量産で既存コメント方針と衝突・腐る・儀式化）／提案をそのまま採用（コメントは検出器であって強制器でないという本質を見ない）／責務コメントを policy/ に置く（コード規約は rules/ が SSOT。[[project_policy_docs]]）／#5 に新スコア行を追加（総点・件数の改番が波及。[[feedback_no_hardcoded_counts_or_enumerations]]）／rubric に載せず rules だけ直す（埋もれて素通り。[[feedback_review_rules_must_be_in_scoring_spine]]）
  - 狙い：「一文で責務を言えるか」は残すコメントでなく設計時の問い・レビューのレンズとして制度化するのが筋（#14 ネスト＝設計シグナルと同型の fold-don't-add）。コメントの強制力の限界を見極め、検出は rubric の背骨に、思考補助は規約のコメント例に振り分けた。失敗モード候補：人間が「コメントで縛る」案を出したとき、それが検出器にすぎず強制力がない点を吟味せずそのまま実装する。関連メモリ [[feedback_review_rules_must_be_in_scoring_spine]] [[feedback_prefer_srp_over_dry_for_docs]]

- 2026-06-30 [correction] アーキテクチャ検査ツール比較で「lint か test か（実行の器）」を差別化軸として重み付けした
  - AI判断：eslint-plugin-boundaries（lint）と ArchUnitTS（テストランナーで実行）を比較する際、「境界違反は振る舞いでなく構造制約だから lint ゲートに置くのが SRP 的に正しい／テストスイートに入れると unit test の定義が滲む」を boundaries 推しの主要根拠の一つにした
  - 人間指摘：テストランナーは実行手段にすぎず、ArchUnitTS の本質も静的解析。lint か test かは差別化点にならない。むしろ今後 AI 生成コードの品質を広く統制したい目的では、カスタムルール＋コード指標（凝集結合）を持つ ArchUnitTS が適しうる
  - 原因：実行ビークルの違い（test runner で起動する）を、概念的・責務的な違いに見せかけて重み付けした。両者が等しく「ソースの静的解析」である本質を見落とし、器の差をツール選定の論拠に昇格させた。加えて、提示された狭いニーズ（境界）だけで結論を出し、ユーザーの広い目的（統制全般）を引き出す前に推奨を確定させた（目的の握りが浅い）。関連メモリ [[project_arch_conformance_detection_gap]]

- 2026-06-30 [decision] optional な未使用引数・プロパティと「不要な optional」の検出を、新スコア項目ではなく既存 #7 の拡張＋agent の専用検出ステップ新設で実現した
  - AI判断：ユーザー要望（①optional で未使用の引数・プロパティが検出漏れ、②optional である必要がないのに `?` を付けたケースも指摘して required 化させたい）に対し、(1) review-criteria #7 を「未使用の変数・引数」から「未使用・不要にoptionalな宣言（宣言が実際の使われ方と一致しているか）」へ拡張し ①② を1項目に畳む、(2) agent に optional 宣言を `\w+\?\s*:` で洗い出し利用箇所を横断確認して「未使用→削除／常に渡される→required化」を判定する専用ステップを新設（既存の export/public 横断検出と同型）、(3) 人間向け guide の観点3（interface）にも「必要以上に optional にしない」を1行追加して採点項目とレンズを揃えた
  - 却下案：#18 として独立スコア項目を新設（総点・件数の改番が波及。[[feedback_no_hardcoded_counts_or_enumerations]]）／①を #7・②を #1 型安全性に分けて格納（同一の「`?` 宣言を列挙→利用箇所を横断確認」という1検出パスで拾えるのに報告先が割れ、agent の手順も二重化）／criteria だけ直し agent に検出ステップを足さない（#7 には元々 Grep 検出ステップが無く visual read 頼みで optional 未使用が漏れていた＝今回の検出漏れの根因なので、spine 文言だけ直しても再発する。[[feedback_review_rules_must_be_in_scoring_spine]]）
  - 狙い：「未使用」も「不要な optional」も〈宣言された引数・プロパティが実際の使われ方と一致しているか〉という単一責務に収まるため #7 に統合でき、spine を増やさず（17項目を不変に保ち）検出力だけ上げられる（#14 と同じ fold-don't-add 方針）。検出漏れの根因が「spine 項目に対応する agent 検出ステップの欠落」だった点を捉え、文言だけでなく検出手順を新設した。失敗モード候補：採点 rubric に項目があると検出されると思い込み、agent 側に対応する具体的検出ステップがあるかを確認しない。関連メモリ [[feedback_review_rules_must_be_in_scoring_spine]] [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-06-30 [decision] code-review-guide の観点3〜5（名前・コメント・interface）を guide では再定義せず SSOT へリンク委譲し、観点1と既存 code-review Skill の衝突を着手前に表面化した
  - AI判断：(1) 「良い命名/コメントの定義」は typescript.md・code-comment-policy が SSOT、review-criteria #2/#9 も既に採点しているため、guide には定義を書かず「レビューでどう当てるか（観点）」だけ置き定義はリンク委譲（三重定義の回避）。(2) ユーザー観点1「自動修正可能な問題はレビュー対象にするな／スタイル違反がレビューに上がるのは仕組みの問題」が、現 Skill の「nit・スタイル違反まで漏らさず厳しく」採点（Import順序#17・未使用変数#7 等 ESLint 領域）と噛み合わない点を、ドラフト着手前にユーザーへ提示し判断を仰いだ。(3) 観点1を原則化する将来の見直し（自動修正ゲート整備＋採点項目見直し）は別タスクとして boy-scout #14 に逃がした
  - 却下案：guide に名前・コメントの良し悪しを書き下す（policy/rules/review-criteria と三〜四重管理になり [[feedback_no_hardcoded_counts_or_enumerations]] のメンテ地獄）／衝突に触れず guide だけ書く（Skill と思想が矛盾したまま放置）／その場で Skill の採点項目を削る（ゲート未整備の今は AI 採点が安全網、[[feedback_review_rules_must_be_in_scoring_spine]]）
  - 狙い：guide は「観点＝レビューのレンズ」に責務を絞り、定義は SSOT に一本化（[[feedback_prefer_srp_over_dry_for_docs]]）。人間向け心構え（Part A）と人間+AI 共通観点（Part B）を分け、Part B のみ Skill から参照させる分業にした。思想の矛盾は黙って実装せず先出しで確認（[[feedback_avoid_overcorrecting_to_feedback]] の逆＝勝手に Skill を直して過剰反応しない）。失敗モード候補：guide 作成時に、隣接する既存 Skill/policy との思想衝突を点検せず素通りする。

- 2026-06-29 [decision] ネスト違反「小手先回避」対策を、新スコア項目ではなく既存 #14 の拡張に折り込んだ
  - AI判断：AIがネスト2重制限を `.catch` 等で見た目だけ平坦化して回避する問題に対し、(1) typescript.md のネスト規則に「違反＝設計を見直せという code smell シグナル」というWHYと小手先回避の禁止を明文化、(2) code-review 側は新規スコア項目（#18）を起こさず既存 #14「ネスト深度」のチェック内容・スコア基準・agent 検出手順（`.then`/`.catch` の Grep ＋文脈判定）に band-aid 検出を折り込む、という構成を選んだ
  - 却下案：#18 として独立スコア項目を新設（「17項目・51点満点」の総点・各所の項目数を全て改番・更新する必要が出る＝メモリ [[feedback_no_hardcoded_counts_or_enumerations]] の件数転記更新コストが膨らむ）／typescript.md だけ直して code-review は触らない（メモリ [[feedback_review_rules_must_be_in_scoring_spine]]：採点 spine に載らない規約は素通りするため検出が機能しない）
  - 狙い：小手先回避は意味的に「ネスト＝制御フローの複雑さ」の一形態なので #14 の射程内に収まり、spine を増やさず（項目数を不変に保ち）検出力だけ上げられる。typescript.md を WHY の SSOT にし、review-criteria/agent からは参照させて二重管理を回避。シフトレフトの理想（issue 段階での検知）は実装中にしか発覚しない性質ゆえ断念し、実装時参照の rules とレビューの2点に置いた（ユーザーの結論と一致）。関連メモリ [[feedback_review_rules_must_be_in_scoring_spine]] [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-06-29 [decision] 「FW/プラットフォームと距離をとれ（ポータビリティ）」の置き場を、ユーザー初期指示の application-design-policy ではなく architecture-policy §1 拡張へ寄せた
  - AI判断：ユーザーは『application-design-policy に追加したい』と指示したが、(1) FW を詳細として境界外へ追い出すマクロ原則は既に application-architecture-policy §1（方針と詳細の分離）に在り、design-policy 冒頭も「マクロ構造は architecture-policy に委譲」と宣言している、(2) 一方 Lambda handler を薄くして本処理を素の関数へ＝コード内部の書き方は design-policy 寄り、と**micro/macro が割れる**ため、着手前に AskUserQuestion で配置を確認。私の推奨は「design 側はコード内部に限定し architecture §1 へリンク」だったが、ユーザーは**architecture §1 を拡張（1節に統合）**を選択。これに従い §1 に `### 詳細と距離をとる（ポータビリティ）` を新設し、FW（流儀へ作り変えを強いる機能は使わない判定テスト）とプラットフォーム（薄いアダプタ＋移植可能な main、code 例）を1節に束ね、過剰抽象を戒める較正（目的は決定遅延、移植性は副産物・原則4/§0）を付けた。既存の「方針と詳細の分離」bullet とは「それをコードの書き方へ具体化」と明示して重複回避
  - 却下案：指示どおり design-policy に新節を立てる（同一概念がマクロ＝architecture §1 と micro＝design に割れて DRY/責務が滲む。design 冒頭の委譲宣言とも擦れる）／design に自己完結で書きマクロ差し替え可能性まで再掲（architecture §1 と二重管理）／私の推奨どおり design に置き architecture へリンク（ユーザーは1箇所に統合を選好）
  - 狙い：FW距離とプラットフォーム距離は『ポータビリティ＝詳細と距離をとる』という同一原則の適用先違いなので、既存のマクロ原則（architecture §1）を拡張して1節に束ねる方が、sibling 文書に並行ノードを作るより SSOT を保てる。配置が micro/macro 境界に跨るときは推測で倒さず確認に回し、ユーザー選好（統合 vs 分離）を反映した。失敗モード候補：単一概念が既存マクロ原則の具体化に当たるとき、指示された文書へそのまま足して原則を二箇所に割る。関連メモリ [[feedback_prefer_srp_over_dry_for_docs]] [[project_policy_docs]]

- 2026-06-28 [correction] 意見を求める質問を実装指示と解釈し、確認なく policy 改訂＋guide 新設まで実行した
  - AI判断：ユーザーの『§4 が浮いている。具体的な決め方は guide として整備するのが良いか？』という*意見を求める問い*に対し、直前の『続けて』も相まって、policy §4 の書き換え・`directory-structure-guide.md` 新設・関連リンク・policy-hub 更新まで一気に実行した
  - 人間指摘：「質問してるだけですよ」——構築の指示ではなく意見を聞いていた
  - 原因：『続けて』を実行指示と短絡し、問いの形（「〜が良いですかね？」＝recommendation を求める発話行為）を読み落とした。相手の発話行為（質問か指示か）を確かめずに行動へ倒す過剰行動。可逆（git）とはいえ、まず答える→合意してから作る、の順を飛ばした。CLAUDE.md「目的を握る／迷えば確認」に反する。関連メモリ [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-06-28 [decision] 新規開発の知識を「薄い policy（Skillなし）」に集約し、フォルダ構成は portable方針のみ切り出した
  - AI判断：ユーザーの『新規開発の知識を専用 policy+Skill にまとめるべきか』に対し、(1) **Skill は作らず policy のみ**（policy は既存 arch-review/check-plan/hook に挿さって効く＝他の全 policy と同じ。stance 宣言に専用 Skill は過剰）、(2) 中核（決定の遅延・スタブ先行・YAGNI）は原則2/4 に既出ゆえ**参照のみ・再掲しない**薄い policy（frontend-design-policy 方式の継承＋差分）、(3) 載せるのは『判断が分かれる我々の既定』だけで教科書的解説は除外（楽観/悲観ロック policy の較正を踏襲）、(4) ユーザー選択の4スタンス（曳光弾=walking skeleton〔同一概念として統合〕・spike破棄・コア着手・フォルダ構成）を採用し未選択の make-it-work-right-fast は入れない、(5) **フォルダ構成は portable方針（Screaming Architecture）だけ policy に置き、具体ディレクトリは設計の領分として design-hub を SSOT に**分離。enforcement は arch-review 観点8（新規開発時のみ・N/A 可）＋check-plan §D の greenfield 行で配線
  - 却下案：policy+Skill 両方新設（Skill は stance に不要・YAGNI）／原則2/4 を policy 本文へ転記（SSOT 違反）／具体ディレクトリ構成まで policy に書く（ポリシーは portable・具体は設計書、というメモリ違反）／全候補を載せる（未選択スタンスの混入）
  - 狙い：policy は『判断が分かれる立場の宣言』、Skill は『繰り返す手順の自動化』——新規開発知識は前者なので policy が器。原則と重なる部分は継承で参照し固有差分だけ書くことで肥大化と転記を同時に回避。portable/具体の線引きはメモリに従い方針=policy・実体=設計書で割った。関連メモリ [[project_policy_docs]] [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-06-28 [decision] arch-review に「決定の遅延」観点を追加する際、原則2を教条化させない条件付けを入れた
  - AI判断：ユーザー要望（新規開発で DB 製品等を早期に固定していないか＝原則2 をレビューさせたい）を、既存の観点1（差し替え*可能性*＝構造）に吸収せず**観点7（決定を実際に*遅らせ*たか＝時間）として独立**させた。さらに観点本文に[!IMPORTANT]較正を付け、「常に何でもスタブにせよ」ではなく**要件・組織標準で確定済み／ドメイン本質の決定は遅らせない（遅延も境界もコスト）**、遅らせる価値があるのに早期固定したときだけ指摘、と非対称に制約
  - 却下案：観点1に吸収（構造と時間の軸が混ざり判定がぼける）／較正なしで「詳細はスタブで遅延せよ」と単純に課す（原則2 TIP・原則4 と衝突し、確定済みDBを無理に遅らせる過剰指摘＝ceremonyを誘発）
  - 狙い：原則から派生させたレビュー観点は、その原則自身の caveat（原則2「揺れうる重要な詳細に絞る・境界もコスト」）と兄弟原則（原則1・4）も一緒に写さないと教条化する。観点の独立性は「赤信号の判定軸が別か」（差し替え可能か vs 早期固定したか）で決めた。失敗モード候補：原則を観点化するとき主張だけ抜き出し制約を落とす。関連メモリ [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-06-28 [decision] /arch-review（設計品質レビュー）の定義の置き方と「条件付きSkill」の配線
  - AI判断：(1) 良い設計/アーキテクチャの定義を principles に置く際、`原則1〜9`の「各原則にトリガー」構造を壊さないよう、番号付き`§0`原則ではなく**イントロ末尾の非番号の枠組み文**として挿入し、新規ドクトリンを足さず既存の原則2(lever)・原則4/まとめ(goal)を**命名・接続**するだけに留めた（principles 肥大化回避）。(2) 具体観点は user 指示どおり SubAgent 側に定義し、各観点が正典 policy を Read して照合する形に（観点6つ＝変更容易性/凝集結合/Screaming/特性均衡/依存侵入/テスト容易性、いずれも application-architecture-policy 等を参照）。(3) SubAgent の責務を「マクロ構造と変更容易性だけ」に明示的に閉じ、doc-review/code-review/validate-design との境界を表で宣言（fat化・重複回避）。(4) 開発フローへ常設せず、check-plan §G では `/arch-review` を**条件付き**扱い——該当変更で未記載なら `⚠️推奨`、無関係なら不問、記載自体を未知Skillとして`❌`にしない、と非対称に配線。model は深い構造判断ゆえ opus（doc-reviewer と同格）
  - 却下案：定義を番号付き§0原則として足す（トリガー構造と不整合・原則扱いの肥大化）／観点を policy 本文に列挙（件数・列挙の転記＝SSOTドリフト [[feedback_no_hardcoded_counts_or_enumerations]]）／check-plan で必須Skillと同じく未記載を`❌`（条件付きを必須化すると無関係変更で誤検知）
  - 狙い：定義は app+infra をまたぐため両者の上＝principles にしか正典を置けない（app-architecture-policy は §6 で infra 射程外を宣言）。ただし principles は北極星ゆえ最小介入が要る→「既存原則の接続」に徹した。レビュー機構は「責務軸＝何を見る目的か」で分け、対象ファイルが同じ（*-design.md）でも doc品質と設計品質は別Skillにする（直前 correction の学習を反映）。条件付きSkillは「種別1:1の必須」と「任意」の中間として ⚠️/不問の非対称ゲートで表現。関連メモリ [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_review_rules_must_be_in_scoring_spine]]

- 2026-06-28 [correction] アーキテクチャ品質チェックの置き場を既存 doc-review への観点追加で提案した
  - AI判断：「設計が変更容易か（UI/DB/言語の差し替え可能性）」のチェックを、*-design.md を既に見ている doc-reviewer-agent §3-3 に1観点足す形で提案した（新規Skillを作らず既存に相乗りさせる案）
  - 人間指摘：doc-review の責務は「文書としての品質」であり、中身であるアーキ・設計の質まで足すと責務が割れて fat になる。専用 Skill + subagent（世界一流アーキテクト・ペルソナ）を新設し、開発フローに常設せず on-demand 実行＋plan段階でAIが必要時に推奨、to-plan の実装フロー記載で事後自動化せよ
  - 原因：新規artifactを増やさない方向（less-is-more/YAGNI）に引っ張られ、SRP の重みを過小評価した。「対象が同じ文書（*-design.md）だから相乗りできる」と捉えたが、レビューの責務軸は*対象ファイル*でなく*何を見る目的か*（文書品質 vs 設計の質）で決まる——軸が別なら同じ対象でも別Skillにすべきだった。既存メモリ [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_review_rules_must_be_in_scoring_spine]] と同型の失敗（責務の異なるものを既存の器に詰める）

- 2026-06-28 [correction] ドキュメントに件数・列挙を転記した（「4つ→5つの問い」を全箇所手修正）
  - AI判断：依存追加ポリシーに問いを1つ足した際、「4つの問い」という**件数**と問いの**列挙**を TL;DR・見出し・他文書（CLAUDE.md / policy-hub / check-plan SKILL）すべてで「5つ」へ手修正した。さらに新設節の本文（L63）が冗長で要点（密結合依存を安易に足すな）がぼけていた
  - 人間指摘：①件数の記入は変更箇所を増やすだけ＝今後一切やめろ②ポリシー追加のたび修正が要る書き方（列挙の転記）は絶対にやめさせたい③L63は意味不明・要点を言い切れ＋再発防止策を考えろ
  - 原因：DRY/SSOT を「同じ説明の重複」だけと狭く捉え、**件数・リストという“増減する派生情報”の転記**もハードコード（修正箇所の増殖）だと認識していなかった。対処：件数を書かず本体（早見表）への参照に置換、L63を「密結合をもたらす依存ほど安易に足さない」と言い切りに、再発防止として documentation-policy「ハードコードしない」に件数・列挙の転記禁止を明文化＋メモリ化 [[feedback_no_hardcoded_counts_or_enumerations]]

- 2026-06-28 [decision] 依存追加ポリシーに「設計に食い込みすぎないか」を⑤として追加（密結合の観点）
  - AI判断：人間要望（ORM 等が Domain Entity と密結合する設計レベルの依存は慎重に決めるべき）を、既存の③ジャングル（推移的依存の*量*）に吸収せず**独立した⑤の問い**として新設。理由は軸が違うから——③は連れてくる荷物の量、⑤は自分の設計（コア）への侵入という質。赤信号の扱いは絶対禁止にせず既存 TIP（代替提示→足すなら隔離境界を設計し根拠を残す）に揃えた（ORM のように密結合でも妥当なケースがあるため二値禁止にしない較正）。「4つの問い」参照を全箇所（dependency-policy/CLAUDE.md/policy-hub/check-plan SKILL）で「5つ」へ追従更新。design-doc-policy・judgment-log 内の別物の「4つの問い」は意図的に非対象とした
  - 却下案：③に1文足して済ませる（量と質の軸が混ざり問いがぼける）／赤信号＝即足さない の二値にする（ORM 採用の正当なケースを潰す）／結合の説明を本節に再掲（DRY違反、application-architecture-policy へリンク委譲すべき）
  - 狙い：観点の独立性は「同じ表に並べたとき赤信号の判定軸が別か」で決める。量的肥大と質的侵入は別判定なので分けた。横断更新では同一文言の他箇所追従と、同名異義（別ポリシーの「4つの問い」）の誤爆回避を両立。失敗モード候補：似た既存項目への安易な吸収で軸を濁す。関連メモリ [[feedback_split_policies_over_merge]] [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-06-27 [decision] 割れ窓（boy-scout）キャプチャの発火点を常時ロードの CLAUDE.md に置いた／当初の「その場で直す」提案を PR 衛生の指摘で撤回
  - AI判断：「無関係な作業中に見つけた割れ窓を失わない」仕組みを、`boy-scout` ラベル付き quick-issue の自動起票として実装。発火トリガーを **policy 文書ではなく CLAUDE.md（常時ロード）** に置いた——偶発的発見は「ポリシーを参照する場面」とAIが認識しないため、参照タイミング駆動の policy 文書では発火しないと判断。運用ルール（①明確な劣化のみ②既存重複チェック③ターン末尾で報告＝確認は省くが無言にしない）を1箇条に凝縮。documentation-policy hook 発火を受け、自分の書いた「暫定措置」を禁止ステータス語と判定し「直すことではなく見えるようにする措置」へ自己修正。会話前半で提案した「些末な割れ窓はその場で直す／別PRで直す」は、人間の「チーム開発では無関係変更がPRに混ざりレビュワーが嫌がる」という指摘で**別PR優先→最終的に issue化のみ**へ撤回
  - 却下案：判断基準ごと専用 policy 文書に書く（偶発検出時に参照されず発火しない＝死蔵）／その場修正やpush-only別ブランチ運用（コンフリクト・WIP混入・レビュー衛生のコストが可視化価値を上回ると人間と合意）／確認を取ってから起票（脱線して捕捉が続かない）／無言で自動起票（人間が気づけない別問題）
  - 狙い：強制・発火は「実際にその事象が起きる文脈」に置くのが効く（依存追加=package.json編集と同じ構図、2026-06-27 dependency-policy エントリと同系）。割れ窓検出は受動・偶発なので、判断基準を能動参照の policy に隔離すると死ぬ→トリガーを常時ロード層へ、詳細基準もユーザー選択で同所に最小凝縮。失敗モード：新機構を設計するとき、それが「いつ思い出されるか」を設計せずルールだけ書く抜け。PR衛生の件は、機構の利便（同一diffで直す）を優先しチーム協働コスト（レビュー・revert・blame）を初手で過小評価した較正ミス。関連メモリ [[feedback_no_process_notes_in_docs]] [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-06-27 [decision] 「クリーンアップ」を1ファイルから同一違反3ファイルへ自律拡張、ただしバックログ削除のIssue化は外部宛先の不確実性ゆえ確認に回した
  - AI判断：ユーザーが指摘した application-design-policy の「整備途上」NOTE だけでなく、grep で同型違反（「記載予定の内容」節・「今後整備」NOTE）を guide 2ファイルにも発見し、確認を取らず一括除去した（計3ファイル6箇所）。一方、削除した将来バックログの Issue 化（documentation-policy が推奨する逃がし先）は、当リポジトリ（claude-dev-template）と CLAUDE.md 記載の issue tracker（pcd-point-bat）が食い違うため**自動起票せず**、控えを回答に転記したうえで確認に回した
  - 却下案：指摘された1ファイルだけ直す（同一違反の放置＝半端なクリーンアップ）／バックログも黙って消す（将来意図の損失）／Issue を自動起票（外部影響を不確実な宛先に確定させるリスク）
  - 狙い：除去は git 復元可能・低リスク・現在仕様を保持するため確認不要のスコープ内と判断（2026-06-17「ハブ追記は言われずともやれ」と同じ較正）。逆に Issue 起票は外部かつ宛先不確実ゆえ「不可逆・公開系は勝手に決めない」に従い確認へ。**同じタスク内でも、低リスク作業は自律実行・外部影響は確認、と可逆性で線を引いた**。関連メモリ [[feedback_no_process_notes_in_docs]] [[feedback_avoid_overcorrecting_to_feedback]]

- 2026-06-27 [decision] 「不要な依存を増やすな」を独立ポリシー新設＋hook主軸で配置し、当初提案したレビューrubric追加を撤回
  - AI判断：依存追加の判断基準を (1) 新規 `dependency-policy.md` に独立配置（既存の application-design/architecture・principles・typescript.md いずれも家として不適と実物確認のうえ判断。SRP優先・全言語横断の理由）、(2) frontmatter `applies-to: ["**/package.json"]` で hook を主軸の強制点に（依存宣言の瞬間に発火＝シフトレフト、実機検証済み）、(3) CLAUDE.md の既存「ライブラリ最新版」行に dependency-policy ポインタを併記、(4) hub に追記。当初ユーザーに提案し承認も得ていた「code-review の採点 rubric に依存項目を足す」は、code-reviewer-agent が `.ts/.tsx` のみをファイル単位で見て `package.json` を一切開かない構造だと実物確認し、**自律的に撤回**
  - 却下案：application-architecture-policy §3（結合）に節として追記（§6で内部構造に閉じると宣言済み・サプライチェーン安全性まで射程外）／typescript.md にルール化（TS/CDK横断に狭すぎ・判断であって機械ルールでない）／承認済みの review rubric 追加をそのまま実装（package.json を見ない reviewer には構造的に無意味で、ファイル単位3点rubricを歪める）
  - 狙い：強制は「実際に判断が起きる点」に置くのが最も効く（依存を書く瞬間＝package.json 編集／plan 段階）。ユーザー承認済みでも、コードベースの事実が前提を覆したら正直に修正する方を優先した（CLAUDE.md「正直に振る舞う」）。レビュー段階の等価物は check-plan 連携として別途確認に回した。関連メモリ [[feedback_split_policies_over_merge]] [[feedback_review_rules_must_be_in_scoring_spine]]

- 2026-06-27 [decision] フロントエンド設計ポリシー新設での「分担・差分」設計と、整合性指摘の解消をどちら側で行うか
  - AI判断：React/TS/Tailwind のフロント整備で、(1) テスト方針は既存（test-strategy-policy・unit-test-policy §9）が統合厚め＋ユーザー視点をカバー済みと判断し**新規テストポリシーを作らず**、未カバーの RTL 固有戦術（クエリ優先順位・userEvent>fireEvent・findBy・スナップショット濫用禁止・MSW）だけを react.md に節として置きリンク委譲。(2) frontend-design-policy は application-design-policy を継承し UI 固有差分（責務分割・状態colocation・Props=IF・デザイン委譲）だけを記述。(3) グローバル状態ライブラリ導入は ADR 送り（YAGNI/決定遅延）。(4) doc-consistency が検出した「親は enum/union 分岐も同類として禁止 vs 子は variant ユニオンを推奨」の矛盾を、**親（application-design-policy）を編集せず子側で「振る舞いの分岐 vs 見た目だけの分岐」を明示**して解消
  - 却下案：フロント専用テストポリシーを新設（§9と重複・SRPでなくDRY違反側）／react.md にテスト思想ごと再掲（SSOT崩す）／矛盾を親ポリシー編集で直す（スコープ拡大・親は既に「振る舞いを切り替えるフラグ」と書いており子の線引き追記で足りる）／状態ライブラリを今ここで選定（前倒しの後戻りしにくい決定）
  - 狙い：既存資産が主題をカバーするものは「リンク委譲」、未カバーの差分だけ新規という分担で DRY/SSOT を保つ。矛盾は「どちらが正か」でなく「両立する区別（presentation only か behavior か）が両文書に書かれていないこと」が実体なので、最小編集で済む子側に区別を足した（親編集はスコープ外・低リスクでも不要な拡大）。状態ライブラリは原則「決定を遅らせる」に従い ADR へ。関連メモリ [[feedback_prefer_srp_over_dry_for_docs]] [[feedback_no_process_notes_in_docs]]

- 2026-06-26 [decision] 仕様書ポリシーをスタブ作成（先のYAGNI見送りを文脈変化＋明示指示で反転）＋クロス参照を整合
  - AI判断：人間の指示で spec-doc-policy.md を新規作成。中身は最小（①想定読者に開発者だけでなくビジネスも入りうる②書き分けは design-doc-policy「迷ったら：仕様書か設計書か」を参照）。指示外だが design-doc-policy の該当NOTEの参照先を documentation-policy から spec-doc-policy へ更新し、policy-hub にも登録（クロス参照の整合）。スタブだが「整備途上/今後追記」等の未来ステータスは本文に一切書かず、現在の仕様（読者前提＋参照）だけで完結させた。人間の「とりあえず」「中身は固まってない」を*作業状況*と解釈し本文に持ち込まなかった
  - 却下案：YAGNIを盾に作成を渋る（文脈が変わった——別の実プロジェクトに仕様書が実在し、人間の明示指示もある）／design-doc-policy側を放置（旧NOTE「仕様書の品質はdocumentation-policyがカバー」が新ポリシーと矛盾する）／スタブに「中身は今後固める」と注記（本セッション2度の同型correctionの再発になる）
  - 狙い：YAGNIは「今要らないものを作らない」であって「指示があっても作らない」ではない。文脈変化（実在）＋明示指示で作成は正当。最小スタブでも未来ステータスを書かず現在形で閉じ、本セッションで2度踏んだ「見送り/整備途上ノート」failure modeを今回は回避できた。ハブ登録・クロス参照更新は低リスク・スコープ内のため確認に回さず実施（2026-06-17 correction「ハブ追記は言われずともやれ」に従う）。関連メモリ [[feedback_no_process_notes_in_docs]]

- 2026-06-26 [correction] 画面遷移を丸ごと設計書に振り分けた（自分の提唱した分解を自分で適用し損ねた）
  - AI判断：仕様/設計の振り分け表で「画面／UI」の設計書欄に『画面遷移・状態の構造』と書き、画面遷移を丸ごと設計書側に置いた
  - 人間指摘：画面遷移はビジネスサイドも気にするので仕様っぽいのでは？（フロント経験は浅いがと前置きしつつ）
  - 原因：直前の自分の主張（迷う項目は3層に分解せよ）を、当の表を作るときに自分で適用しなかった——画面遷移こそ『遷移ルール(操作→遷移先＝観測可能な振る舞い＝仕様)/状態構造・なぜ(設計書)/ルーティング(コード)』に割れるのに、項目名で丸ごと一方に倒した。根因は『図(遷移図)＝設計書』という形式ベースの誤った同一視で、画面遷移図は"仕様を図にしたもの"である点を見落とした。形式(図か文か)と行き先(仕様か設計か)は独立軸なのに混同（"設計書の主役は図表"を"図なら設計書"と裏返しに誤読）。横断パターン：自分が立てた原則を生成物に自己適用しない抜け（2026-06-26『定義文書なのにポリシー体裁を自己適用し損ねた』と同系）

- 2026-06-26 [decision] 仕様書/設計書の振り分け基準を「項目を3層に分解」モデルで設計
  - AI判断：人間の「仕様書か設計書か迷う項目の基準を一緒に考えたい（自分も区別できていない）」に対し、迷いの原因を『項目名が振る舞い(What)/構造・なぜ(How/Why)/具体値 の3層を混在させていること』と診断し、"項目を丸ごと振り分け"でなく"層に分解してから振り分け"る基準を提案。人間の2例で検証（環境変数→なぜ設定軸にしたかは設計書/値はコード、ビジネスロジック→ルールは仕様/流れ・シーケンス図は設計書/コードは実装）し、分解モデル採用と収録例(API・データモデル・UI・非機能/エラー)を AskUserQuestion で確定して新セクション化。3行き先＋ひとことテスト＋具体例表で構成
  - 却下案：項目まるごと一方に割り当てる一覧（env変数等の多層項目で『どっちにも書けない/両方に書く』が出る）／基準を設けず原則文だけ（迷いのブレが残る）／仕様書専用ポリシーを新設してそちらに置く（仕様書はYAGNIで未策定・本書の混入禁止の具体化として設計書側に置くのが自然）
  - 狙い：『仕様 vs 設計』が区別できないのは二者択一で項目を見るからで、What/構造なぜ/具体値の3分解を挟めば各層の行き先が一意に決まる（既存の"書くべきこと①構造②なぜ"分解と同じ刃）。環境変数で人間の直感(設計書)を否定せず『なぜ設定軸にしたか＝設計書／値＝コード』と層で正しさを保存し、丸ごと正誤を付けない。本書の混入禁止(仕様を設計書に混ぜない)を実運用するための判断補助なので、対象外の仕様書側でなく設計書ポリシーに置いた

- 2026-06-26 [decision] 設計書定義の前に「ウォーターフォール的誤読を断つ前提」を置き、一次資料／保守対象を切り分けた
  - AI判断：人間の要望（定義を「コードから読み取れないことを直感的に示した文書」へ変更／具体値だらけ設計書は保守段階で二重管理＝避けるべきと明記／一次資料としての具体値設計書は否定せず本ポリシーは保守される設計書を定義、と説明）を受け、新セクション「前提：本ポリシーが定義するのは保守され続ける設計書である」を**定義の直前**に配置。ウォーターフォール的イメージ（具体値を書き写して実装）を先に名指しで認め、それでは定義が受け取れないと断ってから定義に入る順序にした。一次資料 vs 保守される設計書を ✅/❌ の2行表で切り分け
  - 却下案：前提を定義の後ろor脚注に置く（誤読したまま定義を読み弾かれる）／一次資料への言及を省く（「具体値設計書は全部悪」と読まれ、現場の一次資料作成を否定してしまう）
  - 狙い：新定義は一般的な設計書観（ウォーターフォール＝具体値の束を先に書く）と正面衝突するため、定義を述べる前に読者の既存メンタルモデルを名指しで解除しないと素直に入らない（documentation-policy「読者目線・読者の疑問を先回り」）。誤読リスクの最大点は『具体値設計書＝悪』への過度の一般化なので、一次資料（保守しない記録）は対象外と明示してスコープを限定。抽象原則の列挙でなく『良い/悪いの見分け』を渡す定義文書として、前提も✅/❌対比で可視化した

- 2026-06-26 [correction] 「図表優先」と「なぜを書く」を矛盾と捉え／ポリシーを定義文書として書けていなかった
  - AI判断：(1)「図表だらけが理想」を入れた際、回答末尾で「図だけだと『なぜ』が落ちる、documentation-policyと緊張する」と*矛盾の懸念*として提起し温度感を確認しようとした。(2)ポリシー本文を、原則の列挙（北極星・書くべきこと）として書いた
  - 人間指摘：(1)矛盾しない——WHYは図表の中（注記・吹き出し）に書く。そもそも人は図表しか見ない。主張は「図表だけでよい」ではなく「最低限これしか書けないなら図表が最優先」。(2)このポリシーは『良い設計書とは何か』を定義する文書。読者が「良い設計書／悪い設計書とはこういうもの」と分かる書きっぷりにせよ
  - 原因：(1)「図表」と「文章で書くWHY」を排他と捉え、WHYが図の中に同居できる（図＝箱だけでなく注記を含む表現）ことを見落とした。可視化を『文章を箱に移す』狭い像で持ち、図に意味を書き込む実態を捉えていなかった。(2)ポリシーの目的＝『判断が分かれる事柄に良いXを宣言し見分けを与える』(CLAUDE.md)を、抽象原則の記述に矮小化。『定義文書なら良い例/悪い例の対比で見分けを渡す』という体裁要件（documentation-policyの❌/✅対比・読者目線）を、設計書ポリシー自身に適用し損ねた（ポリシーを説くのに当のポリシー体裁を満たさない自己適用漏れ）

- 2026-06-26 [decision] 設計書ポリシーの中心を「混入禁止＋コードに書けないものだけ」へ再フレーム／「書くべきこと」と「4つの問い」の重複回避
  - AI判断：人間の要望（①設計書に手順・仕様を混入させるなを中心に②書くべきこと＝コードに書けないこと/抽象を定義③具体例を列挙、4つの問いと重複の懸念あり）を受け、北極星を旧「なぜ／変更可能性」から「コードがSSoT→コードに書けない抽象だけ渡す＝手順/仕様/実装詳細を混入させない」へ据え直した。重複懸念は『両者は別軸の2つの半分』と整理して解消——(1)構造・流れ・全体像＝図表で渡す（具体例表）、(2)なぜ＝4つの問いで渡す、と章を分けた。旧「変更可能性」は4つの問いの変更可能境界に温存。具体例は人間提示の3つ（アーキ構成図・シーケンス図・要注意箇所の解説+図解）に、状態遷移図・データモデル(ER)図・依存/境界図を追加。「図表だらけが理想・文章は補足」を明文化
  - 却下案：4つの問いを「書くべきこと」に吸収し1表に統合する（WHATとWHYを混ぜ軸がぼける）／具体例を網羅列挙する（デプロイ図・非機能設計等まで盛ると less is more に反する）／北極星から「なぜ」を外し混入禁止だけにする（4つの問いの土台が消える）
  - 狙い：人間の懸念（重複）の解は「統合」ではなく「別軸への分離」。コードに書けないものは*構造/流れ*（図で渡すのが最適）と*判断/なぜ*（問いで渡す）で渡し方が本質的に違うため、混ぜるとどちらも薄まる。混入禁止表（手順→runbook/仕様→spec・test/実装詳細→code）で「中心に混入禁止」の要望を満たしつつ、各記述に「コードを読めば分かるか？」の単一テストを与えてSSoT原則を運用可能にした。具体例追加は人間が「他にもあれば」と明示的に招いたためコードから見えない抽象に限って厳選

- 2026-06-26 [correction] YAGNI見送りの「未来ステータス」を設計書ポリシー本文に書いた（CLAUDE.md L40違反・当日2度目の同型）
  - AI判断：design-doc-policy 本文に「仕様書専用ポリシーは…仕様書が実在し品質がブレてから足す（YAGNI）」「未策定（※下記）」と書いた。仕様書を見送る判断（前エントリ）を、本文に未来ステータスとして残した
  - 人間指摘：CLAUDE.md L40 を参照（本文は現在の仕様のみ。「整備途上・今後追記・暫定」等の未来ステータスを書くな）
  - 原因：CLAUDE.md L40 明文ルール＋メモリ feedback_no_process_notes_in_docs ＋**同日同セッションの先行 correction（整備途上note）** がありながら再発。失敗モードは『YAGNIで見送る判断を下すと、その"見送りステータス"を当該文書の本文に書き残す』という固定パターン。L40の禁止語リスト（旧/廃止/今後/暫定…）に「YAGNI」「未策定」が字面として無いため射程外と誤認した（言葉尻マッチで精神に従わない、2026-06-26整備途上noteと完全同型）。見送りの経緯は判断ログに残れば十分で、本文は現在形（本書は設計書専用・仕様書はdocumentation-policyがカバー）だけで足りた

- 2026-06-26 [correction] 設計書ポリシー内で仕様書のスコープを矛盾させた
  - AI判断：design-doc-policy で「対象は設計書、仕様書は対象外」と冒頭宣言しながら、別の[!NOTE]に「仕様書も本書を準用する」と書いた。さらに hub 概要・applies-to は設計書のみのため、仕様書を書く人が辿り着けない動線になっていた
  - 人間指摘：仕様書作成時にも参照する前提の記述になっているか？ hub に仕様書の言及がないなら参照されないのでは？
  - 原因：YAGNIで仕様書専用ポリシーを見送った穴を「準用」で埋めようとし、(1)同一文書内のスコープ宣言との矛盾、(2)本書固有部（なぜ/変更可能性・却下案）が仕様書に移植できない事実、(3)hook/hubの動線、を照合しなかった。『見送ったものを軽い一文で取り繕う』癖。実態は documentation-policy が `**/*.md` で仕様書を既にカバーしており、本書を準用させる必要自体が無かった（前提を確かめず接ぎ木した）

- 2026-06-26 [decision] 設計書ポリシーの粒度・固有性・仕様書の扱い
  - AI判断：「良い設計書を定義するポリシー」を、仕様書/設計書の2本に割らず**設計書1本**に絞り、共通品質は documentation-policy へリンクして再掲せず、設計書固有の差分（北極星＝なぜ／変更可能性、答えるべき4つの問い＝採用理由/却下案/変更可能境界/スコープ外、経緯はADRへ）だけを書いた。仕様書ポリシーは仕様書が実在しないため見送り（YAGNI）。命名は application-design-policy（アプリを設計する）との衝突を避け `design-doc-policy.md`（設計書を書く＝runbook-policy の対）に。固定目次（雛形）は人間の指摘どおり設けず、「縛るのは目次でなく答えるべき問い」とした
  - 却下案：仕様書＋設計書の2本に分割（SRP最大だが共通部の重複＋未存在文書への投機）／1本に両者を同居（北極星対比表で書き分け）／セクション雛形を design-hub に置く
  - 狙い：runbook の前例（documentation-policy 継承の独立ポリシー）に倣いつつ、まだ無い仕様書まで先回りで作るのは原則4/5（YAGNI・less is more）に反する。共通品質の転記は SRP を理由に増やせるが、本件は「設計書という1ジャンル内の差分」であり documentation-policy リンクで足りると判断（過去 2026-06-23 correction の『目的が異なる文書は転記』は別ジャンル間の話で、本件の継承関係には当たらない）。固定目次は documentation-policy「対象読者と目的が全てを決定する」と矛盾するため、問いベースの背骨に置換。粒度・分割・YAGNI 判断は AskUserQuestion で確認のうえ確定

- 2026-06-26 [decision] 「良いインターフェース」原則の文書内での位置づけ
  - AI判断：application-design-policy に「正しく使うのが一番易しいインターフェースにせよ」節を新設する際、既存節（引数は責務を映す鏡・関数とクラスの使い分け・失敗は投げて伝えよ）が既にこの原則の具体的適用例だと見抜き、新節を**冒頭付近の上位「傘」**として置き、適用例は**散文の相互リンク**で繋いだ（表で各節を側面に1対1対応づけはしなかった）。確認法（呼び出し側を先に書く）は判断原則3シフトレフトの具体化としてサブ節化。適用範囲はコードのインターフェースに限定しUIは対象外と明記
  - 却下案：既存節と並列の独立セクションにする／適用例を `側面→節` の表にする／UIまで含めて文書スコープを広げる
  - 狙い：傘構造にすれば既存の具体例を重複再掲せず原則を一度だけ宣言できる（DRY）。適用例を表にすると「引数節は誤用困難だけに効く」等の偽の1対1対応を生む（実際は複数側面に効く）ため散文に留めた。範囲・配置・統合深さは原則から一意に出ず文脈依存のため AskUserQuestion で確認（→ コード限定・傘＋相互リンクを採用）

- 2026-06-26 [decision] callout への宛先・強制力ラベル一括付与の判定基準
  - AI判断：docs/ 全 callout にラベルを付与する際、(1) 宛先は既定で `AI`（ポリシーは「AIの出力ブレを抑える」ために在る＝AIが操作対象の読者、と CLAUDE.md が宣言）とし、人間にも明示的に宛てた箇所だけ `/人間にも` を足す、(2) 強制力は callout 種別から機械写像（IMPORTANT/WARNING→必須、TIP→推奨）、(3) ラベルを付けるのは行動を促す callout のみで、TL;DR・「最重要原則3つ」recap・引用/出典・背景/状態の NOTE はスキップ、と決めた。スキル(.claude/skills/)・CLAUDE.md は対象外にした
  - 却下案：宛先を既定 `AI・人間` にする（大半が両対象なので正確）／全 callout に機械的に付与（NOTE・TL;DR含む）／スキル・CLAUDE.md まで全 .md に適用
  - 狙い：`AI・人間` 既定は正確だが冗長で信号が薄れる（原則5）。ラベルの主目的は「AI 指示か人間向け強調かの判別」なので、操作対象＝AI を既定に置けば短く一貫し、判別目的を満たす。万一 `AI・人間` 既定が正なら sed 一括置換で可逆なので、低コスト側に倒した。スキル/CLAUDE.md は文書全体が既に AI 指示でラベルが冗長になるため除外（"全て" の指示に対し、規約の less is more を優先した自律判断）

- 2026-06-26 [correction] 設計書本文に過程の記述（整備途上note）を残した＝CLAUDE.md明文違反
  - AI判断：database-design-policy 冒頭に「整備途上・現状は同時更新制御のみ・今後追記」のNOTEを置いた。さらにこれを指摘された際、判断ログには「削除テスト未適用／書き手の都合を読者価値と取り違えた」という*ソフトな判断ミス*として記録した
  - 人間指摘：CLAUDE.md は「設計書本文は現在の仕様のみ・過程の記述を残すな」と明確に主張している。なぜ違反したのか
  - 原因：CLAUDE.md「ドキュメント・コメントの基本方針」の明文ルールへの違反。違反した根因は、同ルールの禁止キーワード例（旧・廃止・移植・以前・もともと・リファクタ）が**すべて過去向き**だったため、ルールを「改定経緯（バックワードな履歴）の禁止」と狭く解釈し、「整備途上・今後追記」という**前向きのステータス記述**を射程外と扱ったこと。原則（本文＝現在の仕様のみ）ではなくキーワード一覧にパターンマッチした（言葉尻に従い精神に従わない失敗モード）。加えて、最初の自己分析が「削除テスト」という弱い基準に逃げ、*明文ルール違反*という最も強い原因を見落とした（matchするルールの能動的な探索不足＝2026-06-25「rules見落とし」と同系）
- 2026-06-26 [correction] ハブ概要を現トピックに特化して書いた
  - AI判断：policy-hub の database-design-policy 概要を「現状は楽観/悲観ロックのみ」と現トピックを列挙して書いた
  - 人間指摘：今の記述に特化しすぎ。今後トピックが増える前提でもっと抽象的に（文書の役割で）書くべき
  - 原因：書いた時点の内容（1トピックしかない状態）に視点を固定し、文書の寿命＝将来の拡張を考えなかった。ハブ概要を「文書の役割」ではなく「今ある中身」の要約にしてしまい、トピック追加のたびに概要を書き換える保守負債を作った（メンテ容易性＝変更が予想されるものをハードコードしない、の文書版）

- 2026-06-26 [decision] 楽観/悲観ロックのポリシー適格性の切り分け
  - AI判断：「楽観/悲観ロックの使い分けをポリシー化すべきか」に対し、テーマを「教科書的に決まる工学知識」と「グレーゾーンを潰す私たちのデフォルト立場」に二分し、ポリシーに載せるのは後者（＝価値判断）だけだと整理した。その上で database-design-policy を「既定は楽観ロック・悲観は限定条件＋短時間保持」という判断基準のみで構成し、ロック機構の解説（知識）は一切書かなかった。可視化は判定が単一分岐のため逆変換テストで図を見送り表＋TIPに留めた
  - 却下案：使い分けの決定木をそのまま解説込みで書く（リファレンス化）／立場を決めずに中立併記する
  - 狙い：プロジェクトのポリシー定義（判断が分かれる事柄に「私たちの良いX」を宣言し出力ブレを抑える）に照らすと、決定木の解説は知識でありポリシーの価値（デフォルト立場による迷いの解消）を生まない。知識とポリシーを混ぜると文書が膨らみブレ止めが薄まるため、価値判断のみに絞った。構成依存（Lambda等）は断定せず原理レベルに保ち、過去correction（2026-06-17 設計ポリシーは汎用）とメモリ（ポリシーはポータブル）に整合させた

- 2026-06-25 [decision] 無価値な図（文章の言い換え）への対策の置き場
  - AI判断：可視化の質低下を「可視化を増やす圧力（怠慢である等）に対し、質を測るブレーキが無い非対称」と診断。policy にはテキストの削除テスト・表の表化テストはあるが図に同等のテストが無い欠落を埋め、名前付き「逆変換テスト」を新設。①policy（定義＋人間向け一言）②doc-reviewer-agent（検出ロジック＋採点行を量だけ加点から質込みへ修正）③design-doc-mermaid（描く前の Value Gate）の3層に配置した
  - 却下案：policy に一言の方針文だけ足す（生成・レビューで強制されず素通りする）／検出ロジックだけ足し採点行は触らない
  - 狙い：方針文だけでは「埋もれたチェックは素通りする」（メモリ feedback_review_rules_must_be_in_scoring_spine）。定義を北極星として置きつつ、採点rubricに質を載せて後ろ盾にし、最上流の生成スキルにも自己ブレーキを置く多層防御にした。既存の削除テスト・表化テストと同じ「名前付き捨てる基準」の語法に揃え、policy 内の一貫性を保った
- 2026-06-25 [decision] レビュースキルが rules を見落とす欠陥の修正方式
  - AI判断：code-review が import順序違反を素通りした原因を「採点rubricに項目が無い＋ルール照合ステップが条件付き・曖昧・報告に出口なし」という構造欠陥と診断。import順序は採点項目に昇格（背骨化）、ルールファイルの必須読込＋全項目照合＋報告セクション必須化で多層に固定。CDK側は差分検知ルール8項目を rubric の採点項目化（34項目への大改修）ではなく「Critical ゲートとして報告」する方式にした
  - 却下案：import違反だけ1項目足して終える（対症療法）／CDK差分検知ルールも全部スコア項目化して減点対象にする
  - 狙い：症状（import）ではなく失敗の構造（rubricに無い規約は能動的に探されない）を断つのが目的。レビューの背骨は「採点項目＋報告フォーマット」で、ここに乗らない規約は埋もれる。一方で安全規約（差分検知）は減点で薄まるより合否ゲート（Critical）の方が強く効くため、スコア化と使い分けた。ルールファイルを唯一の正として毎回読ませることで、rubric転記のドリフトも回避
- 2026-06-25 [correction] エラーコード採番・カタログの要否判断
  - AI判断：エラーコード採番を「ロギング寄りで重い機構」と捉え、本文には入れず末尾パーキングのみを提案した
  - 人間指摘：エラーコード一覧は「システムで起きうるエラーを一望できる」カタログとしての価値がある。常にやるべきではないにせよ、その観点が抜けている
  - 原因：採番（書式・採番ルールという重い機構）とカタログ（失敗モードの網羅目録）を一塊で見て「重い／ロギング」と片付け、後者の設計価値（網羅チェック）を取りこぼした。価値を分解せず一括で deferral に倒した（2026-06-22「表化を全構造へ過剰一般化」と逆向きの、一括で切り捨てる失敗モード）
- 2026-06-24 [decision] エラーハンドリング方針の判断軸選定
  - AI判断：throw 統一の使い分け基準を、人間が挙げた「異常 vs 想定内の失敗」軸ではなく「呼び出し側にとって throw が役に立つか」軸で設計した。さらに「throw か戻り値か（機構）」と「Result型かネイティブunionか（表現）」を2層に分離し、戻り値表現を認める狭い例外でも Result/Either 型は禁じた
  - 却下案：人間提案どおり「異常 vs 想定内」を主軸にする／単純に「Result型禁止」だけを宣言する
  - 狙い：「異常 vs 想定内」軸だと想定内の失敗がすべて戻り値表現に倒れ、AIの「なんでもResult」を正当化してしまい目的（throw既定）と矛盾する。AIのResult濫用の本体は機構ではなく表現選択（Result型に手を伸ばす癖）なので、2層に分けて表現側を塞ぐのが効くと判断。人間の初期フレーミングを尊重しつつ、目的に照らして軸を組み替えた（合意済み）
- 2026-06-24 [decision] 引数ポリシー節のタイトル命名と収録範囲
  - AI判断：節タイトルを「引数は責務を映す鏡」とし、数上限・ブーリアン引数アンチパターンの2点を「引数は責務を映す」という1つの WHY で束ねた。マジック値禁止は収録、出力引数の回避と primitive obsession は除外した
  - 却下案：2ルールを並列に列挙するだけ（束ねる WHY を立てない）／出力引数・primitive obsession も網羅的に収録する
  - 狙い：既存節（レイアウト〜・上位関数〜）が「1つの格言＋複数の下位指針」構造で、WHY を語る作りに揃えるため一意。primitive obsession は「引数の数・形」ではなく型設計の領分で焦点がぼけるため除外、出力引数は同系だが節の主軸（責務の切り替え）から外れるため見送り、スコープを絞る判断を優先した
- 2026-06-24 [correction] 「100行程度に削減」を上限と解釈し過剰削減
  - AI判断：「100行程度に削減」を受け、126行を84行まで削った（言い換えTIPの除去に加え、共通化の①②小見出し＋テーブルを統合するなど構造も圧縮）
  - 人間指摘：「程度」は100行前後の意味。上限と解釈するのは普通におかしい
  - 原因：「削減してほしい」という方向性に引きずられ、「程度＝おおよその目標値」という語義を取り違えて「上限」に倒した。少ないほど指示に忠実という思い込みで、目標値からの下振れもズレであることを照合しなかった（指摘への過剰反応で反対の極端へ振れる失敗モード＝2026-06-14「フォーマット粒度」と同系）
- 2026-06-24 [correction] スキルのフロー配置を前提条件と矛盾する位置に提案
  - AI判断：pr-check（既存PRを対象に gh pr view/comment で動くスキル）を、CLAUDE.md 開発フローの Step7-8（コミット・push前）に位置づける選択肢として提示した
  - 人間指摘：このスキルはPR作成前でも使えるのか？（＝その時点ではPRが無く動かない。配置が成り立たない）
  - 原因：選択肢を提示する際、その対象物（スキル）自身の前提条件（PRが存在すること）と配置先（PR作成前）が両立するかを照合しなかった。フロー上の「コミット前後」という語感だけで位置を決め、artifact の動作前提に立ち返らなかった。設計合意の中で確定済みの制約（PR前提）を、別観点の判断（フロー配置）に持ち越せていない
- 2026-06-24 [decision] 街灯効果アンチパターンの配置とDRYトレードオフ
  - AI判断：街灯効果を「何を監視するか（根拠を持って選ぶ）」節の中、GSM推奨TIPの直後・ブラックボックス節の前に「オオカミ少年」と並ぶアンチパターン小節として置いた。例の良い側は `決済完了率` をGSMガイドと統一し、悪い側に既存「根拠」表と重なる `CPU` を含めた
  - 却下案：質を保つ後半節（オオカミ少年の隣）に置く／既存「根拠」表とCPU例が重複するので街灯表の例を全面差し替えする
  - 狙い：街灯効果は「取りやすいデータへの引力」＝選定の歪みであり、質（アラートの鋭さ）ではなく選定（何を測るか）の問題なので前半節が一意。既存表との軸の違い（根拠の有無 vs バイアスの誘因）が明確で、`PR数・行数` 追加で運用外への一般性も示せるため、CPU重複は許容しDRYより概念の対比を優先した（良い例の統一は読者の追跡負荷を下げるDRY側の配慮）
- 2026-06-24 [correction] TL;DR に行を追記して4行に膨らませた
  - AI判断：pr-review-policy に作成者前提の項目を足すついでに、TL;DR へ新たな箇条書きを1行追加し4行にした
  - 人間指摘：TL;DRは3行までが絶対ルール
  - 原因：本文へのセクション追加に意識が向き、TL;DR編集時に documentation-policy「絶対に3行まで」を照合しなかった。既存3行に新情報を畳み込む（サイズ行へ200行を内包）選択肢を、行追加より先に検討しなかった。ポリシー適用 hook が出ていたのに、可視化・DRY は確認した一方で同ポリシー内の TL;DR 制約を見落とす部分的照合になった
- 2026-06-24 [decision] 関数とクラスの使い分け基準の主軸選定
  - AI判断：application-design-policy に使い分け節を起こすにあたり、判断軸を「状態と不変条件を外部から守る必要があるか」の1本に絞り、「既定は関数 + モジュール、クラスは明確な理由があるときだけ」というスタンスを既定にした
  - 却下案：軸を「ポリモーフィズム/差し替えの要否」や「テスト容易性」に置く／関数・クラスを中立併記して既定を設けない
  - 狙い：既存原則（明示>暗黙・シンプル志向・コードは負債）から「関数既定」は一意に導けるため、軸選定と既定スタンスは自律で決めた。一方、配置（本体 vs 独立サブポリシー）とドメインモデルの厚みは原則から一意に出ず文脈依存のため AskUserQuestion で確認（→ 本体の一節・ドメイン方針は未定につきクラス側は中立記述に留めた）
- 2026-06-23 [correction] 新ポリシーを既存ポリシーへリンクで繋ごうとした
  - AI判断：手順書ポリシーの共有原則（なぜを書く・シンプルさ等）は documentation-policy にリンクし、固有分だけ書く（DRY優先）を推奨した
  - 人間指摘：このプロジェクトは DRY より SRP を優先する。目的が異なる文書は独立・単体完結にし、documentation-policy の使える部分はリンクせず転記せよ
  - 原因：文書間の重複を一律に DRY 違反と捉え、SRP（目的の異なる文書は単体で使えること自体が価値）との優先順位を確認せず、DRY を既定の上位に置いた。手順書と設計書が「目的が異なる別ジャンル」である点を、重複削減より優先すべき軸として扱えなかった
- 2026-06-22 [correction] 可視化ポリシー強化で表化を全構造へ過剰一般化
  - AI判断：「構造のある情報はデフォルトで表にする」と既定値を反転し、doc-review ゲートにも表化必須指摘を追加した
  - 人間指摘：それだと図にすべき箇所（関係・流れ・階層・状態遷移）まで表になりかねない
  - 原因：「可視化」と「表化」を同一視し、表化の既定値を構造一般へ過剰一般化した。元の可視化トリガーが構造ごとに図と表を振り分けていた事実を踏まえず、表だけを既定形に格上げした（ルール強化時に対象範囲を広げすぎる失敗モード）
- 2026-06-21 [correction] 検証ゲートをポリシー化しようとした
  - AI判断：「マージしてよいの定義（検証ゲート / DoD）」を pr-review-policy の双子として新規ポリシー化する方向で設計・確認した
  - 人間指摘：強制できるゲートは GitHub Actions ＋ブランチ保護で機構的に強制すればよく、ポリシー文書は不要（設定と二重管理になる）
  - 原因：policy（人によって判断が分かれる「良いX」の宣言）と mechanism（客観判定で強制できるチェック）の境界を引き違えた。merge gate の大半はバイナリ判定可能＝機構の領域なのに、判断残余として扱いポリシーに寄せた。「文書化＝整備」と捉え、強制できるものは設定が SSOT という DRY 観点（documentation-policy）を後回しにした
- 2026-06-19 [correction] 「ハードコードしない」の対象を取り違え
  - AI判断：ポリシー強制 hook の「ハードコードしない」を *どのファイルにどのポリシーを当てるかの対応表* と解釈し、マッピングをどこに置くか（frontmatter / 外部JSON）を主論点に据えて確認した
  - 人間指摘：避けたいのはポリシーの *中身*（旧 hook が ①②③ と要約を直書きしていた）を script に書き写すこと。対応表（パスのポインタ）は論点ではない
  - 原因：「ハードコード」の指示対象を確認せず、自分が想定した論点（対応表の保守）に寄せて解釈を固定した。元プロンプトの「policyの内容や〜」の *内容* を読み落とし、二重管理の核（本文⇄scriptの同期）を後回しにした
- 2026-06-19 [correction] シフトレフトの自発提案漏れ（目的適合の仕組み化）
  - AI判断：目的に合わない記述の混入対策を、文書作成・レビュー段階（write-time 自己監査・doc-reviewer ゲート）中心に設計し、最左の工程（grill-me / plan ＝タスク検討段階）への前倒しを自分からは提案しなかった。check-plan への組み込みは人間が提案して初めて出た
  - 人間指摘：refined-engineer-judgment-principles に「シフトレフト」が最上位原則として在るのだから、AI 側から開発フロー初期段階での対応を提案すべきだった。原則が機能していない
  - 原因：シフトレフト原則のトリガーが「後で確認・修正すればいいと思ったとき」という*先延ばしの感覚*に限定され、*対策・チェックの置き場所を設計するとき＝もっと左に置けないか*という面を捕捉しなかった。omission（考慮漏れ）型の失敗は自己トリガーでは捕まりにくい
- 2026-06-17 [correction] ポリシー汎用/特化分類で「設計ポリシー」を特化判定
  - AI判断：cdk-design / database-design / github-actions-design-policy を、対象（AWS・DB・CI）が固有という理由で「特化」に分類した
  - 人間指摘：いずれも汎用。技術をうまく使うための設計原則（良いCDK/DB/GHAとは）であり移植可能
  - 原因：ファイル名の名詞（技術領域）に引きずられ、中身の性質（"-policy"＝原則レベルで移植可能か、固有スペックか）で判定しなかった。memory「CI/CD設計依存は汎用に含めない」を、原則レベルのGHA設計方針にまで過剰適用した
- 2026-06-17 [correction] ハブ追記を確認に回した
  - AI判断：policy-hub への monitoring-policy 追記を「他ポリシーも未掲載なのでまとめて直すか要相談」として確認に倒した
  - 人間指摘：言われなくてもやっておいて（低リスクかつ明らかにスコープ内）
  - 原因：分割作業の副作用である自明な配線を「独立した判断事項」と誤認し確認コストを過小評価。過剰確認の失敗モード（2026-06-14 目的確認エントリと同系）
- 2026-06-17 [decision] 汎用ポリシーへのAWS具体例の混入
  - AI判断：monitoring-policy の actionable 節で、原則文は汎用（「クラウド基盤障害」）に保ちつつ、AWS（Lambda・AWS Health）を明示した具体例を Admonition に収録した
  - 却下案：メモリ「ポリシーは汎用・ポータブル」に従い具体名を一切出さない／全面的にAWS前提で書く
  - 狙い：ユーザーが具体例（AWS明示）を明示選択した一方、メモリは汎用性を要求。原則＝ポータブル・例＝具体、と層を分けることで両要求を両立させた（流用時は例だけ差し替えればよい）
- 2026-06-14 [correction] 目的確認指示の設計（CLAUDE.md）
  - AI判断：目的確認の指示を「目的が一意に読めず、かつ目的次第でやり方が変わるとき」に限定した
  - 人間指摘：目的確認は何より重要。執拗に・最優先で確認させるべき
  - 原因：過剰確認という別の失敗を恐れ、先回りで絞りすぎた（確認コストを過大評価し、目的取り違えのコストを過小評価）
- 2026-06-14 [correction] 判断ログのフィールド設計
  - AI判断：3番目の項目を「教訓（一般化した原則）」とした
  - 人間指摘：本質がズレる。捕捉すべきは「判断ミスの原因」では
  - 原因：一般化＝分析の産物を捕捉時にやろうとした（早すぎる加工）。原因の方が生信号で目的に直結すると見抜けなかった
- 2026-06-14 [correction] 判断ログのフォーマット粒度
  - AI判断：判断ログのエントリを「起きたこと1文＋教訓」まで圧縮した
  - 人間指摘：削りすぎ。AI判断と人間指摘の差分が判断基準づくりの核
  - 原因：「重い」という前回指摘に過剰反応し、反対の極端へ振れた。目的に必要な信号かを確認せず一律に削った
- 2026-06-14 [correction] 判断ログのフォーマット設計
  - AI判断：判断ログのエントリ形式を6フィールドで設計した
  - 人間指摘：記録が重すぎる。捕捉時は最小限にせよ
  - 原因：網羅的・丁寧＝良い記録と思い込み、大量蓄積時の1件あたりコストを軽視した
- 2026-06-14 [correction] 判断ログのデータ源
  - AI判断：feedbackメモリだけで原則抽出できると考えた
  - 人間指摘：全ての矯正がメモリに残るわけではない（メモリは加工後成果物）
  - 原因：メモリが「加工後の成果物」である性質を見落とし、生データの保存層と混同した
